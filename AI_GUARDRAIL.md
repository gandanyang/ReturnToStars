# AI 施工纪律（AI_GUARDRAIL）

> 用途：给 AI 的"缰绳"。本文件是开工前的强制检查，与 `AGENTS.md`、`docs/AI开发前必读.md` 配套使用。
> 适用：所有参与《归星物语》开发的 AI（Codex / Trae / opencode / WorkBuddy 等）。

---

## 一、定位声明

**你不是制作人。**

- 不要主动扩大需求
- 不要"顺手"加功能、加系统、加剧情、加资源
- 你的价值是把制作人拍板的事做好，而不是发明新的事

---

## 二、开工前必答三问

修改任何东西之前，先回答：

1. **当前问题是什么？**（不是"我觉得可以更好"，而是具体问题）
2. **玩家是否能感知？**（改动是否落在玩家可见/可感的地方）
3. **是否影响 15 分钟体验？**（是否让首轮体验更完整）

如果答案不是"明显提升"，**不施工**。

---

## 三、禁止事项

未经制作人明确批准，禁止：

- ❌ 新增系统
- ❌ 新增剧情
- ❌ 新增资源
- ❌ 重构架构

---

## 四、优先级（固定顺序）

```
修 Bug > 提升反馈 > 美化 > 新功能
```

- **修 Bug**：最高优先，影响稳定性/体验的问题
- **提升反馈**：给玩家明确、及时的反馈（音效/文案/演出/视觉反馈）
- **美化**：让已有内容更好看、更有质感
- **新功能**：最低优先，且必须制作人明确要求

---

## 五、与项目规范的衔接

- 详细开发规则见 `AGENTS.md`（开工必读、测试分级、Git 协作）
- 门禁检查见 `docs/AI开发前必读.md`
- 架构入口见 `docs/开发约束与架构入口.md`
- 冲突时：**制作人最终决策 > 顶层设计.md > 功能规划文档 > Agent 自主建议**

---

## 六、并发修改规则（多 AI 协作 · 2026-08-08 制作人拍板）

> 背景：2026-08-08 出现 StationScene.ts 被并发会话改坏（重复粘贴、语法错误）导致 tsc 全挂 + 游戏页面崩溃的事故。

### 硬规则

1. **禁止多个 Agent 同时修改同一个核心文件**。核心文件包括：
   - `src/scenes/MapScene.ts`
   - `src/scenes/FarmScene.ts`
   - `src/scenes/StationScene.ts`
   - `src/systems/StorySystem.ts`
   - `src/systems/SaveSystem.ts`
   - （以及任何单文件 >500 行的核心模块）
2. **必须并行时**：先划分文件责任（谁改哪个文件写清楚），避免同一文件双写。
3. **禁止**：A 修改文件 → B 基于旧文件覆盖提交（覆盖 = 丢失 A 的工作）。

### 操作纪律（开工/收工必做）

4. 改核心文件前：`git status` 检查目标文件是否已被他人改动（`M` 状态且非自己改动 → 先停下确认归属）。
5. 发现他人编辑中的**损坏代码**（语法错误/半成品）：不擅自动，先报告制作人；获授权才修。
6. 提交前：确认目标文件没有被其他会话改过（`git diff` 只含自己的改动）。

---

## 七、一句话

> 稳定 > 新功能；体验 > 系统数量；完成一个闭环 > 添加十个半成品。
