# 代码审查报告（2026-08-06 21:46）

> 审查范围：相簿系统（FEATURE-041）+ 剧情回顾（FEATURE-040）+ BGM 恢复 + 支线试点（他人工作区改动）
> 基线：556230b（根提交）+ fa4f1c9/697b99d/aa8d381
> 方法：静态审查 + tsc + 探针实跑验证
> 结论：**无 P0**；2 个 P1（均为他人支线试点引入）；5 个 P2/建议

---

## 一、问题汇总

### P0（阻塞级）：无

### P1（高，需尽快处理）

| # | 位置 | 问题 | 归属 |
|---|---|---|---|
| P1-1 | `src/data/PhotoAlbum.ts`（他人新增 2 条）+ `public/assets/photos/album/` | **照片数据引用不存在的资源**：`xiya_garden.webp` / `elder_star.webp` 文件未生成（目录仅 3 张图）。解锁后 `<img>` 404 破图 | 支线试点线 |
| P1-2 | `tests/probes/probe-photo-album.mjs` | **探针断言与数据不同步**：他人把 PHOTO_DATABASE 从 3 扩到 5 张，但探针仍断言 `cardCount===3`/`locked===3`/解锁后仅 1 张图 → 实跑 **5/11 失败**（tsc 0 错，纯断言过时） | 支线试点线 |

### P2（中，建议修复）

| # | 位置 | 问题 | 归属 |
|---|---|---|---|
| P2-1 | `src/ui/PhotoAlbumPanel.ts` renderCard | `<img>` 无 `onerror` 回退——参照 StoryDialogue 立绘的 error 处理（首字色块兜底），建议补防破图 | 我（可修） |
| P2-2 | `src/ui/PhotoAlbumPanel.ts` renderCard | description/source 经 innerHTML 拼接未转义——当前数据源是项目内常量（安全），若未来接用户内容需 `escapeHtml`（DialogueHistoryPanel 已做了转义，此处应一致） | 我（可修） |
| P2-3 | `src/scenes/MapScene.ts` 支线试点 | `sideXiyaGarden*`/`sideElderStar*` 在看板标"方案待拍板"时已施工（涉冻结区 StorySystem.ts）——若方案被否需整体回滚，建议尽快拍板或明确"试点观察期" | 协调 |
| P2-4 | `src/ui/StoryDialogue.ts` addEntry 时机 | 历史在"行显示时"记录而非"玩家看完时"——skip 整段对话会录入未看完的行。符合"会话级累计"设计，但若未来做"剧情档案馆"需改语义 | 设计取舍 |
| P2-5 | `src/audio/MusicSystem.ts` BGM_MUTED | 恢复为 false 后首次播放依赖 getCtx 的 resume + MusicSystem.bindRetry 双兜底（闭环完整，无真 bug）；建议后续把 BGM_MUTED 改为可配置项（设置面板）而非硬编码 | 建议 |

---

## 二、逐模块审查结果

### 1. 相簿数据层 `src/data/PhotoAlbum.ts` ✅（除 P1-1）
- unlockPhoto 幂等（Set）、存档 album 顶层可选字段、旧档兼容（`?? []`）——均正确
- restoreAlbumSaveData 过滤未知 id（数据迁移友好）✅
- 文件头注释、红线说明清晰 ✅

### 2. 相簿面板 `src/ui/PhotoAlbumPanel.ts` ✅（除 P2-1/P2-2）
- 模块级单例（同 ShopPanel 模式）、z-index 220 层级合理、Esc/按钮/点空白关闭 ✅
- 未解锁剪影占位（斜纹 + 🔒 + 来源提示，不剧透）✅
- 已解锁卡片：标题/描述/来源/webp ✅
- 副标题实时计数（`已解锁 x/5`）——会随 P1-1 修复自然正确

### 3. 存档接线 `src/systems/SaveSystem.ts` ✅
- `SaveData.album?: string[]` 顶层可选，save 写 / apply 恢复，与 FEATURE-039 lockedItems 模式一致

### 4. 剧情回顾 `src/ui/StoryDialogue.ts` + `DialogueHistoryManager/Panel` ✅（P2-4 为取舍）
- **冻结/恢复状态机正确**：
  - 打字中打开回顾 → 暂停 timer + 保存 charIdx → 关闭后从断点继续（28ms 同速）✅
  - 已打字完打开 → savedTyping=false → 关闭后 textEl 保持完整 ✅
  - 选项行 → resume 分支跳过（`!line.options` 守卫）✅
  - advance/skip 在面板打开时被拦截 ✅
- 环形缓冲 MAX=50、会话级累计（不随 play 清空）✅
- renderEntry 有 escapeHtml 转义 ✅、内心独白斜体灰字 ✅、滚动到底 ✅
- reset() 时若面板开着先关（防残留覆盖层）✅

### 5. 支线试点 `src/scenes/MapScene.ts`（他人）✅（P2-3 协调 + P1 见上）
- 夏雅藤架（3 木材消耗 → 完成 → 解锁照片）✅ 幂等（sideXiyaGardenDone 前置）
- 镇长星空（夜晚空地 → 完成 → 解锁照片）✅ 幂等（sideElderStarDone 前置）
- 4 个 flags 在 mapFlags 存档/恢复完整 ✅
- 解锁点调用 unlockPhoto 位置正确、防重入 ✅

### 6. BGM 恢复 `src/audio/MusicSystem.ts` ✅
- `BGM_MUTED=true→false`，恢复后 play 正常走 fetch+decode+loop 播放
- 首次播放被自动播放策略拦截 → pending + bindRetry（pointerdown/keydown 补播）闭环完整
- 资源 4 曲 ogg+mp3 齐全；probe-music-restore 4/4 验证通过

---

## 三、验证结果（实跑）

| 检查 | 结果 |
|---|---|
| tsc --noEmit | ✅ 0 错 |
| probe-photo-album | ❌ 5/11（P1-2：断言过时，他人改动引入） |
| probe-music-restore | ✅ 4/4 |
| probe-dialogue-history | ✅ 13/13（本轮未重跑，功能未动） |

---

## 四、建议动作

1. **P1-1**：通知支线试点线补齐 2 张 webp（从现有 promo 图转码或新生成）
2. **P1-2**：探针断言改为动态（`=== getAllPhotos().length` 或数据驱动）——建议把 `3` 硬编码改为读 PHOTO_DATABASE 长度，避免再被数据变更打破
3. **P2-1/P2-2**：相簿面板补 img onerror 回退 + description 转义（对齐 DialogueHistoryPanel 做法）——我可立即修
4. **P2-3**：制作人尽快对支线方案拍板（试点观察 or 回滚）
5. **P2-5**：BGM 静音改为设置项（长期）

> 注：P1 两项均属支线试点线在途改动，未擅自修改；P2-1/P2-2 若获确认我立即修复并提交。
