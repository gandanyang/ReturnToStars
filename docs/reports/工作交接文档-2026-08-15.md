# 归星物语 · 交接清单（2026-08-15 收工压缩版）

> 用途：压缩当前上下文，供下一会话/新 Agent 直接接手。完整细节见各设计稿与收口清单。

---

## 一、项目概况与硬约束（不可违反）

- 像素风治愈乡村 RPG（Phaser 3 + TS + Vite），移动端**仅横屏**，目标安卓。根目录 `G:\ReturnToStars`。
- **多 Agent 并行**：工作区有大量未提交改动（含并行 Agent 在途）。禁止解除/回滚他人改动。
- **无 Git 写操作**：`git add`/`commit`/`reset` 一律禁止（提交由 opencode/制作人执行，见 `docs/reports/收口清单-2026-08-15.md`）。
- 已拍板决策优先于历史文档（`docs/DESIGN_DECISIONS.md`，今日新增 D-031/D-032/D-033）。
- tsc 应保持绿（`npx tsc --noEmit`）。
- 核心文件单写入：`MapScene.ts` / `SaveSystem.ts` / `EventManager.ts` 同一时间只允许一个 Agent 改。

## 二、最近完成（今日批次，均已验证）

1. **种植生活化**：切片 B 番茄架（`crop_tomato_xiya_seen`→farm 番茄架）；v1.4「土地回应系统」（farm ≥3 格成熟→`crop_field_alive`→蝴蝶蜜蜂+夏雅一句+小梅菜园线）。
2. **钓鱼生态化 v1.3**：小鱼苗低概率事件（15%，放回/带走）；放生→2 天后河面鱼影+老姜台词；特殊鱼河鳗/鲤鱼/大青鱼（60/35/90G）+老姜三句定稿；漂流瓶锁星星线（D-033）。
3. **邮箱系统 v1.0**：`grandpa_gift_opened` 后 farm 老屋门口信箱；首封爷爷演出；2-3 天随机来信+世界事件信插队；`src/data/MailLetters.ts`（定稿信件）+ `src/ui/MailboxPanel.ts`。
4. **farm 房子**：2D 门脸建模（三角屋顶+墙+门窗+地基+阴影，两态）；通路修复（移除屋内两棵树+打开南/东/西墙，保留北面门洞）。
5. **出口 bug**：farm→town 无限回跳修复（`exits.ts` 出生点 (7,18)→(9,18)）。
6. **图鉴墙美术**：展示架+缩小+移到花田西南侧 (4,10)。

## 三、关键文件

- 场景：`src/scenes/MapScene.ts`（大文件；新增区域：番茄架 `setupTomatoTrellis`、土地回应 `checkFieldAlive/setupFieldLife`、放生 `releaseCurrentFish/presentFryReleaseChoice`、邮箱 `setupMailbox/updateMailQueue/tryMailboxInteract`、房子 `buildOldHouseRuined/Restored`（v4 2D 门脸）、图鉴墙 `buildInsectWall`）。
- 数据：`src/data/MailLetters.ts`（新）、`FarmState.ts`（`countGrownTiles`）、`Economy.ts`（特殊鱼价）、`Inventory.ts`（鱼苗+特殊鱼）。
- 面板：`src/ui/MailboxPanel.ts`（新）。
- NPC：`src/systems/NPCSystem.ts`（小梅菜园线）。
- 地图：`public/assets/maps/farm.json`（房子通路已改）。
- 探针：`.tmp/probe-{crop-trellis,crop-field-life,fish-release,mailbox,farm-town-exit}.mjs`（全绿）。

## 四、并行 Agent 活动

工作区含其他 Agent 未提交改动（采集/树视觉/居民需求系统已在提交 269b1cf；仍有在途文件）。改 `MapScene.ts` 前先查 git status/文件 mtime 确认无并发。

## 五、已知问题 / 注意事项

- `probe-fishing-phase1.mjs` 有 1 个既有失败（T7 过时断言，非回归）。
- farm-restore 探针后半段 `keyboard.emit` 崩溃是探针与 Phaser 版本既有问题。
- 房子建模后续还要再优化（制作人待办）。
- 老姜特殊鱼台词、邮箱信件文字已定稿，如需配音走语音流程（先试听再打包）。
- 邮箱/信件相关语音尚未接入。

## 六、下一步（制作人方向）

1. 收口提交（`docs/reports/收口清单-2026-08-15.md`，由 opencode/制作人执行）。
2. 房子建模再优化。
3. 真机试玩：邮箱节奏、房子观感、钓鱼手感。
4. 后续大方向：丰收节（种植系统大展示）仍放后面；星星线（漂流瓶）锁住不施工。
