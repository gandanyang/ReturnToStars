# 工作区改动交接清单 v1.0（2026-08-30）

> 性质：细粒度交接（逐文件改动 / 提交命令 / 语音接入步骤）。会话级总结与经验沉淀见
> [会话工作总结-2026-08-30-走查修复落档.md](会话工作总结-2026-08-30-走查修复落档.md)。
> 用途：ZCode 会话（走查 + 修复 + 试玩准备）工作区交接。按 Git 协作规则，提交统一由 opencode/制作人执行。
> 状态：全部改动已回归验证（`tsc` 0 错误；相关探针全绿，见 §四）。

---

## 一、本轮 ZCode 修复（两个批次，8 个代码文件）

### 批次 1：走查修复（P0×2 + P1×5 + P2×1）

| 文件 | 改动 | 严重度 |
|---|---|---|
| `src/modules/CutsceneGuard.ts` | 新增 `reset()`（旗标跨重启卡死类根修） | P0/P1 |
| `src/scenes/MapScene.ts` | `create()` 开头调 `guard.reset()`；`startSpringFair` 补 runner 守卫 + 忙时重试；3 处 triggerOnce 内嵌 save 挪到返回后；`handleBackButton` 补音乐盒/需求板/邮箱/等待面板；睡觉路径 4 处裸 setTimeout 改 delayedCall+守卫 | P0/P1 |
| `src/systems/SaveSystem.ts` | `hasSave()`/`deleteSave()` 包 try-catch（localStorage 被禁环境标题页黑屏） | P0 |
| `src/ui/StoryDialogue.ts` | `skip()` 补 `isOptionLine()` 守卫（选项行不可被 Skip 绕过） | P1 |
| `src/ui/WaitPanel.ts` | 三重事件绑定改单 pointerdown + open 守卫（双触发修复） | P2 |

### 批次 2：P1 体验债（制作人试玩清单三项）

| 文件 | 改动 |
|---|---|
| `src/scenes/MapScene.ts` | 新增 `R2()` 触屏半径补偿，34 处交互半径触屏下统一 ≥34px（桌面不变）；`canTryCatchButterfly` 钓鱼中让位；新增公开 `getFishingStats()` 委托 |
| `src/modules/FishingController.ts` | casting/waiting 按 E 主动取消（原忽略）；无钓点防死态；新增会话统计埋点（casts/successes/failsEarly/failsTimeout/cancels/咬钩反应时长，**不入档**） |
| `src/audio/MusicSystem.ts` | play 异步链加请求令牌（修快速切图 BGM 竞态） |
| `src/audio/VoiceBank.ts` | play 加请求令牌（修快进对话语音叠音）；缓存改 LRU 上限 40 |
| `src/main.ts` | 新增 `window.debug.fishingStats()` 埋点钩子（服务钓鱼手感验收 4 问） |

### 探针修复

| 文件 | 改动 |
|---|---|
| `tests/probes/probe-fishing-edge.mjs` | 跟随 P5c 迁移改走 `fishingController`（探针自身债务，修复前带病运行）；E5 断言更新为"主动取消"新契约；E8 currentFish 固定时机修正 |
| `tests/probes/probe-music-box.mjs` 等 3 个 | 显式设置 `return_star_sound_on=1` 消除静音默认探针债（box/cross-scene 在 goto 后设，restore 在 clear 后重载前设） |

### 批次 3：P2 清债（制作人提交/试听确认等待期间，2026-08-30 下午）

| 文件 | 改动 |
|---|---|
| `src/systems/DailyQuestSystem.ts` | 防重入改 `questsReadyForDay` 旗标（原条件依赖列表长度，坏档清空列表时同日重刷破防；⚠️ 此文件属在途第二章混合改动） |
| `src/modules/WeatherDirector.ts` | `make.tilemap` 只读 widthInPixels 后立即 destroy（修复每次下雨泄漏一个 Tiled Tilemap） |
| `src/scenes/MapScene.ts` | `cleanupSceneDom` 补关 shop/音乐盒/爷爷包裹/邮箱/等待面板（防跨场景残留打开态 + panel_open 冻结）；作物选择器 ESC 处理器泄漏修复（`cropPickerEscHandler` 引用统一移除）；SHUTDOWN 钩子改每实例只装一次（修 farm↔town 切换监听器累积）；`toggleSound` 关声音时补 `VoiceBank.stop()` |
| `src/ui/MusicBoxPanel.ts` | 移除生产路径 `[MBdiag]` console.warn 调试残留 |
| `tools/apply_voice_retrial.py`（新增） | **语音接入一键脚本**（§三 自动化）：默认 dry-run、`--apply` 执行、旧文件自动备份到 `tmp/voice_apply_backup/`、sidecar 文本校验（`--fix-sidecar` 应对 adv_01 本地化旧 sidecar）、转码参数与 convert_voice_ogg.py 一致。**制作人试听确认后执行 `python tools/apply_voice_retrial.py --fix-sidecar --adv-take N --apply` 即完成接入**，随后跑 probe-voice / probe-bug039-voice-sync 回归 |
| `src/modules/WeatherDirector.ts` + `tests/probes/probe-life-loop-day.mjs`（批次 3 追加） | L1 预存失败根修：探针读的 `scene.rainActive/rainOverlay` 是 P3 天气迁移前的旧字段（迁移起恒 undefined→L1 必败），WeatherDirector 补公开只读 getter `hasRainVisuals`（`isRaining` 已有），探针改走公开 API → **probe-life-loop-day 9/9**。天气检查触发时机本身无问题（setTime 跨小时下一帧即跑 hourly 块） |
| `src/scenes/MapScene.ts`（批次 3 追加） | 村长来访演出覆盖风险根修：`ch1_elder_visit` 在演出开始前标记，1.2s 敲门延迟内若对白被占用 → play 静默覆盖 → endVisit 永不执行（村长精灵残留/选择不入档）。修法同春日集范式：入口忙时不触发（下次进屋重试）+ 延迟回调忙时每秒重试（上限 30 次，超限放弃演出仍走收尾存档）。回归：house-tidy 12/12、market-square 15/15（消费 ch1ElderChoice）、full-story 48/48、ch2-return 8/8 |
| `src/scenes/MapScene.ts` + `public/assets/maps/farm.json` + `tests/probes/probe-ch3-lighthouse.mjs`（新增）+ `tests/probes/probe-ch2-return.mjs` + `tests/probes/probe-ch3-keeper.mjs`（新增）+ `tests/probes/probe-ch3-ship.mjs`（新增，13/13）+ `tests/probes/probe-ch3-finale.mjs`（新增，15/15）（批次 6-9，制作人指令"继续制作第三章内容 包括新地图"，2026-08-31） | **第三章 幕一「灯塔开门」+ 幕二「执灯人三件套」+ 幕三「来船靠岸→记录计划→镇民注脚」完整闭环 + 幕四（碎片×3 渐进发现：灯塔内→栈板尽头→海湾缺口，日记段2/段3 指引；灯室结算·C1 谜底方向=爷爷不是预言，是相信「总会有人回来」）+ 幕五（归位窗口·夜21+ → 三结局行为承载·无选项面板：留岛=睡床 / 返城=上船 / 桥=日志留灯塔+守灯到天亮 →「第三章·归位——完」）+ 地图工程**（任务卡 `docs/tasks/任务-第三章幕一-灯塔开门-v1.0.md` 含幕二/幕三节；预埋方案 §四 第三层落地）：出口解锁门禁（ch2_black_dot）+ farm 西墙打通（四字段合规）+ 海湾视觉 + 灯室亮起/夜晚光束 + 执灯人 + 初见演出 + **幕二深交互**（日志三选一续写留痕 / 望远镜观察 / 铜铃 / 执灯人日常+半句话）+ **幕三开局**（来船靠岸演出·性质留白 / 外来船+旅人常驻 / 旅人日常 3 句）。探针：幕一 **8/8**、幕二 **8/8**、幕三 **13/13**、幕四+五 **15/15**、ch2 回归 8/8。规划件同批：`docs/design/第三章剧情大纲-归位-方向稿-v0.1.md`、`docs/design/老周心语任务-灯下有痕-剧情规划-v1.0.md`、`docs/reports/会话工作总结-2026-08-30-走查修复落档.md`、`docs/reports/游玩剧情路线-v1.0-2026-08-30.md` |
| `src/scenes/MapScene.ts` + `tests/probes/probe-ch1-spring-fair.mjs`（批次 4，制作人报告"春日集剧情有 bug"） | 春日集一次性剧情可永久丢失根修：① 占用超限时 endSpringFair 释放旗标 → 已标记的剧情永久丢（灯火亮了没人说话）→ 改无限重试 + `springFairStoryOwed` 欠播补播（切图/重启后重进 town 补播，含 FX 重建）；② 触发只在进镇后 1s 查一次 → 白天进镇天黑未走永远错过 → update 加 60s 低频重查。新增探针 S5a/S5b（owed 补播 + 旗标释放）→ **7/7** |
| `src/scenes/MapScene.ts`（批次 5，第二章打标审计关差距） | ① **夜谈（全章高潮）打断丢失隐患根修**：`ch2NightTalkActive` 无 create 复位 + 标记先行无补播 → 切图打断即高潮永久丢失；加 `ch2NightTalkOwed` 欠播补播 + create 复位 + FX 死引用过滤（同春日集范式）。② **节拍 3 内容补齐**：基线"之后码头开始有人走动（晾网、搬筐）"氛围层缺失 → `setupCh2PierLife()`（晾网绳+晾网人+背筐走动剪影，修复后常驻，零资产）。回归 probe-ch2-return **8/8** |

> ⚠️ **提交注意**：`src/scenes/MapScene.ts` 与 `src/main.ts` 是**混合改动**（在途第二章施工 + 本轮修复在同一文件），建议整文件入库或逐 hunk 核对。其余 6 个文件（CutsceneGuard/SaveSystem/StoryDialogue/WaitPanel/FishingController/MusicSystem/VoiceBank）在本会话开始前未被修改，改动纯为本轮修复。

---

## 二、工作区在途第二章施工（非本轮，他人/先前会话产物，未提交）

**src（8 文件）**：`DevTestHub.ts`、`StorySequenceRunner.ts`、`DailyQuestSystem.ts`、`NPCSystem.ts`、`StorySystem.ts`、`QuestPanel.ts`（+ MapScene/main 混入部分）——对应第二章《花期未至》/「故人远来」施工，`probe-ch2-return` 8/8 绿。

**docs（30+ 文件，modified + untracked）**：第二章剧情大纲《故人远来》方向稿、心语二《花期未至》stage 兼容边界/口径校准稿、任务卡（第二章节奏表拆解 v1.0）、CURRENT_TASK / DESIGN_DECISIONS 更新等。

**tests（untracked 探针 8 个）**：`probe-ch2-return`、`probe-ch0-station-visual`、`probe-ch1-busline-smoke`、`probe-diag-farm-tutorial`、`probe-sleep-diag×3`、`probe-sleep-freemode`；另有 tests/reports 快照更新。

**建议提交顺序**：① 在途第二章施工一批（src+docs+新探针，制作人对内容拍板后）；② 本轮走查修复一批；③ 探针修复随 ② 走。

### 可直接执行的提交命令（2026-08-30 晚追加；遵守协作规则：逐路径暂存、禁 add -A）

```bash
# ── 提交 1：在途第二章施工（纯在途文件，制作人拍板内容后执行）──
git add src/dev/DevTestHub.ts src/modules/StorySequenceRunner.ts src/systems/NPCSystem.ts src/systems/StorySystem.ts src/ui/QuestPanel.ts
git add docs/design/ docs/tasks/ docs/CURRENT_TASK.md docs/DESIGN_DECISIONS.md docs/dev/TestSystem.md docs/reports/ docs/制作人控制台.md "docs/MapScene重构-P8归档-CutsceneGuard统一门控.md"
git add tests/probes/probe-ch2-return.mjs tests/probes/probe-ch0-station-visual.mjs tests/probes/probe-ch1-busline-smoke.mjs tests/probes/probe-diag-farm-tutorial.mjs tests/probes/probe-sleep-diag.mjs tests/probes/probe-sleep-diag2.mjs tests/probes/probe-sleep-diag3.mjs tests/probes/probe-sleep-freemode.mjs tests/reports/
git commit -m "feat(ch2): 第二章在途施工 + 剧情文档（故人远来/花期未至口径）+ 探针"

# ── 提交 2：本轮走查修复 + P1/P2 清债 + 探针修复（含 3 个混合文件，见下注）──
git add src/modules/CutsceneGuard.ts src/modules/FishingController.ts src/modules/WeatherDirector.ts src/systems/SaveSystem.ts src/ui/StoryDialogue.ts src/ui/WaitPanel.ts src/ui/MusicBoxPanel.ts src/audio/MusicSystem.ts src/audio/VoiceBank.ts
git add src/scenes/MapScene.ts src/main.ts src/systems/DailyQuestSystem.ts
git add tests/probes/probe-fishing-edge.mjs tests/probes/probe-music-box.mjs tests/probes/probe-music-cross-scene.mjs tests/probes/probe-life-loop-day.mjs tests/probes/probe-ch3-lighthouse.mjs tests/probes/probe-ch3-keeper.mjs tests/probes/probe-ch3-ship.mjs tests/probes/probe-music-restore.mjs tests/probes/probe-life-loop-day.mjs
git add tools/apply_voice_retrial.py "docs/reports/工作区改动交接清单-v1.0-2026-08-30.md"
git commit -m "fix: 走查修复 P0×2/P1 + P1 体验债（触屏半径/音频竞态/钓鱼取消）+ P2 清债 + 探针跟随重构"

# 第三章（幕一~三 + 灯塔半岛开放 + 剧情大纲/灯下有痕规划）随提交 1 的 docs/design/ docs/tasks/ 通配
# 与提交 2 的 probe-ch3-*.mjs / MapScene.ts 入库；建议提交后追加 tag v0.8-ch3-wip。

# ⚠️ 混合文件说明：MapScene.ts / main.ts / DailyQuestSystem.ts 同时含在途第二章改动与本轮修复，
# 逐 hunk 拆分不现实——随提交 2 入库（其内容已全部回归验证）。
# 提交前核对：git status 确认无他人新改动卷入；提交后打 tag（建议 v0.7-ch2-wip）。
```

> 其余未列出文件（若有）= 制作人本地工作产物，自行判断归属。`tmp/`、`logs/`、`test-results/` 均已 gitignore，不会入库。

---

## 三、语音重录交付（试玩-07 / 试玩-12，先试听后接入）

产物目录：`tmp/voice_retrial_20260830/`（gitignore 内，不污染仓库）。**全部为试听候选，未接入正式管线**（未动 `art_source/audio/voice/` 与 `public/`）。

| 条目 | 角色 | 台词 | 候选文件 | 时长/校验 |
|---|---|---|---|---|
| station_04 | 林澈 | 至少这次，是我自己选的离开。 | `linche_station_04.wav` | 1.73s（0.14s/字，略快于筛查带下限 0.15，属短情绪句，请听判） |
| garden_01 | 花匠小梅 | 你好呀，我叫小梅。这些花都是我亲手种的，漂亮吧？ | `gardener_garden_01.wav` | 3.69s（0.18 ✓） |
| garden_02 | 花匠小梅 | 你爷爷以前每天下午都会来闻这株花的味道。他说这和城市的空气不一样。 | `gardener_garden_02.wav` | 4.99s（0.16 ✓） |
| adv_01 | 阿风 | 嘿！还记得我不？小时候后山那一圈，就是我带你跑熟的。 | `adventurer_adv_01_take1/2/3.wav` | 3.23s / 2.39s ×2（**重点听：是否念完整句**，见下方说明） |

**参考音**：林澈=林澈新B青年清澈（项目定案）；小梅=千早爱音中文S5（项目定案）；阿风=阿风阳光A（08-06 库内；原参考 `E:\BINGdown\...\男磁性.MP3` 已缺失）。

**⚠️ adv_01 已知问题**：部分参考音下模型在第一个问号分句后提前停（自参考/对话E/旧参考均复现，产出 ~2s 截断段）；阳光A 参考下方差大（同输入 3.2s~10s）。已出 3 个 take 供挑选；**若全部不合格**：① 制作人启动 IndexTTS REST API（`api_server.py --port 8000`）后用 `/api/tts` 重试，或 ② 改用 MiniMax T2A 保底。三个作废 take 已删除，仅保留正式候选。

**试听通过后的接入步骤**（制作人确认后再做）：覆盖 `art_source/audio/voice/<role>/<tid>.wav`（+sidecar txt 核对）→ `convert_voice_ogg.py` 转 ogg 入 `public/audio/voice_normalized/` → `probe-voice` / `probe-bug039-voice-sync` 回归 → 更新《问题追踪.md》试玩-07/12 闭环。

---

## 四、回归验证记录（2026-08-30）

| 项 | 结果 |
|---|---|
| `npx tsc --noEmit` | 0 错误（每批次后均跑） |
| probe-ch1-spring-fair | 5/5 |
| probe-save-restore | 24/24 |
| probe-skip-debounce | 3/3 |
| probe-night-fatigue-hint | 5/5 |
| probe-mobile-sleep | 全步骤 ✅ |
| probe-fishing-phase1 | 34/34（含埋点代码在场） |
| probe-fishing-edge | 23/0（含新"取消"契约） |
| probe-fishing-phase4 | 25/25 |
| probe-play-story-integration | 24/24 |
| probe-interaction-gates / cantry-pure / targets | 全过 |
| probe-dialogue-history | 13/13 |
| probe-ch2-return | 8/8 |
| probe-music-box | **16/16**（修复静音前置后由 6/10 转绿） |
| probe-music-restore | **4/4**（由 2 败转绿） |
| probe-music-cross-scene | **7/7**（由 1/6 转绿；A2~A5 跨场景"我的歌"连续性同时端到端验证了 MusicSystem 令牌改动） |
| probe-rain-snail | 8/8 |
| probe-life-loop-day | **9/9**（L1 曾 8/9：根因=探针读 P3 迁移前旧字段，非天气时机问题；已修复转绿，修复过程端到端验证了 WeatherDirector 含 tilemap destroy 在内的雨链路） |
| probe-action-time / gather-flow / house-tidy / bug035-gate-skip | 6/6、34/34、12/12、6/6 |
| probe-ch1-afeng-welcome / dryyard / artshow-aftermath / market-square | 12/12、35/35、14/14、15/15 |
| probe-xiya-bloom | 43/43 |
| probe-full-story-run | **48/48**（完整主线链：triggerOnce 时序/演出/对白跳过/面板生命周期全链路，覆盖本轮全部改动面） |
| probe-gate-skip | T1 起级联失败——**预存**（stash 对照基线证实同败；属 CURRENT_TASK 已登记的"试玩-14 门禁探针任务态前置脆弱性"家族：完整主线任务态在隔离探针环境下的已知限制，建议留完整主线环境验证，与本轮改动无关） |

**存档影响**：零 `SAVE_VERSION` 变更、零字段增删；钓鱼统计为会话内存不入档；全部旧档兼容。

---

## 五、交接后待制作人事项

1. **提交收口**：按 §一/§二 分批暂存提交（8+9 个代码文件 + 30 余 docs + 8 新探针）
2. **语音试听**：§三 四条（重点 adv_01），确认后通知接入
3. **实机试玩**：钓鱼 Phase 1 手感 4 问（试玩后 `window.debug.fishingStats()` 读会话数据辅助判断：casts/successes 比、failsEarly/failsTimeout 构成、reactionMsAvg 判断 0.8s 收竿窗口松紧）；Action Time 一天节奏压力测试（`debug.consumeMinutes/getTimeStr/getStamina` 钩子已齐备）
4. **后续债**（已确认未修，见问题追踪）：BUG-007 竖屏画布、BUG-037/039 真机复测、E-10 响度归一、仓库卫生（根目录冗余 ogg / maid.md / set_fish_key.cmd 敏感面）
