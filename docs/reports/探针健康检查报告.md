# 《探针健康检查报告》

> 范围：tests/ 下 46 个测试文件（45 .mjs + 1 .cjs）
> 方法：逐探针与基准源码核对 + **实测修复前/后对比验证**（git stash 旧版基线 → 新版）
> 日期：2026-08-03 ｜ 状态：**7 个须修探针已全部修复并验证（5 真问题 + 2 原判定为误判，详见 §三）**

## 一、须修探针（已全部修复并验证 ✅）

| 优先级 | 探针 | 原判定 | 实测结论 | 修复内容 | 修复后验证 |
|---|---|---|---|---|---|
| P0 | probe-npc-life-acceptance.mjs#L97 | ~~崩溃~~（data 一维） | **误判**：Phaser 3.80 `layer.data[tileY][tileX]` 为二维数组，`data[18][1]` 正常返回 Tile；修复前实测 8/8 全绿 | 改公开 API `wallsLayer.getTileAt(1,18)`，不依赖内部结构（稳健性改进） | ✅ 8/8 |
| P0 | test-ch1-story.mjs#L295 | ~~必然失败~~ | **误判**：短行打字自动完成后 advance 直接翻行（StoryDialogue.ts#L219-235），实际推进快于 `N*2`；选项行拦截多余 advance；修复前实测 24/24 全绿 | `skipDialogue(12→13)` 加安全余量；L199 `12→11` 对齐真实行数 | ✅ 24/24 |
| P0 | probe-b1-island-report.mjs#L136 | 恒真假绿（title/desc 不匹配） | **确认**：正则匹配 title（'拜访镇长'），面板渲染 desc（'与镇长对话'） | 改匹配 desc 短语；删除 L122-129 死代码 | ✅ 13/13 |
| P0 | probe-first-hour.mjs#L107/108/133/134/147 | 5 处恒真占位断言 | **确认**：`check(名, true, 说明)` 全部空转 | 补真实断言（FarmState API / CROP_DEFS / gardenRestore / dawnXiya / 环境音） | ✅ 15/15 |
| P1 | probe-mobile-ux.mjs（整文件） | 失败也绿（无退出码） | **确认**：仅 console.log，❌ 后仍 0 退出 | 加 pass/fail 计数 + `process.exit(1)` | ✅ 8/8 |
| P1 | probe-density-v053.mjs#L185 | 边界推进不足 | **确认**：MINER 实为 16 行 | `skipDialogue(14→17)`；GARDENER 注释 11→12 行；**另修复 2 个潜伏 bug**：① E4b 假设"小梅 10:00 在森林"实为 14:00 起（NPCSystem 日程）；② 每日随机句 regex 只覆盖 6 句池中 4 句（seed 取模选句，覆盖不全会碰运气） | ✅ 9/9 |
| P1 | probe-ambience.mjs#L114-119 | 恒 true 兜底分支 | **确认**：未集成分支 `check(..., true, true)` 恒绿 | 恒 true 分支改为真实轮询断言（集成验证 AmbienceSystem.start）；修正 check 第三参数误用（detail 误当 warn 标志） | ✅ 9/9 |

> 修复原则：**只修探针逻辑，零业务代码改动**（7 个探针文件仅改断言与注释）。

## 二、可延后（记录）

| 探针 | 问题 |
|---|---|
| probe-automation.mjs#L238 | 恒真项，但 #L241-243 已有真实错误断言兜底；#L149-151 itemCount 死代码 |
| probe-density-experience-v053.mjs | 无任何断言（仅对话走查+截图），功能全坏也"通过" — 建议标注"人工走查探针" |
| probe-npc-daily-action.mjs#L144 | `d.miner ? true : false` 实为存在性检查 |
| 注释过时 | test-ch1-story.mjs#L199（"12 行"实为 11）；probe-density-v053.mjs#L204 |
| 死代码 | probe-elder-portrait.mjs#L41 |

## 三、误判修正说明（重要：测试体系可信度的第一课）

探针健康报告原判定 2 条 P0"崩溃/必然失败"，**经实测均被推翻**：

1. **"probe-npc-life-acceptance#L97 会崩溃"** ❌ → 实测修复前 8/8 全绿。
   根因：报告假定 Phaser 3.80 TilemapLayer.data 是一维扁平数组，但源码 [GetTileAt.js#L26](file:///c:/Users/Gdy/Documents/trae_projects/mihoyoStarPlanting/node_modules/phaser/src/tilemaps/components/GetTileAt.js#L26) 明确 `layer.data[tileY][tileX]` 是二维数组。
2. **"test-ch1-story#L295 三选项断言必然失败"** ❌ → 实测修复前 24/24 全绿。
   根因：报告按"每行严格 2 次 advance"推算，但 [StoryDialogue.ts#L219-235](file:///c:/Users/Gdy/Documents/trae_projects/mihoyoStarPlanting/src/ui/StoryDialogue.ts#L219-L235) 短行打字自动完成后 advance 直接翻行，实际推进更快；且选项行拦截多余 advance，多按无害。

**教训**：纸面推断 ≠ 实测。探针是否可信，只能靠真实跑一遍判定——这正是本轮修复的动机。

## 四、本次核对基准（修复引用）

- skipDialogue 语义：每行最多 2 次（打字机+翻行）；短行打字自动完成后 advance 直接翻行，实际可更快；`skipDialogue(N)`=N*2+1 次；选项行 advance 被拦截
- 行数基准：XIYA=13、GATE_OPENED=7、SOW=7、WATER_CROPS=6、EVENING=7、TOWN_INTRO=5、ELDER_QUEST=11、DEMO_ENDING=15（含选项行 index=14）、BRANCHES 各 4、FINALE=5
- NPC 对白：**MINER=16、GARDENER=12**、ADVENTURER=8、SHOPKEEPER=4、MYSTERY=7、MYSTERY_AFTER=3
- talk 任务：title='拜访镇长' / desc='与镇长对话'；QuestPanel 渲染 desc
- Phaser ^3.80：`layer.data[tileY][tileX]` **二维数组**；公开取瓦片 API `getTileAt(x, y)`

## 五、全量回归记录（2026-08-03 补跑）

覆盖 tests/ 全部 46 个文件（7 个已修探针 + 39 个其余），分批并行实测（每批 5 个，独立日志 + 退出码）。

**结果：43 全绿 + 2 登记待重写 + 1 无断言走查（probe-density-experience，exit 0）**

> 更新（2026-08-03 晚）：**2 条待重写已按 `{plot,crop}` 新结构重写完成并复跑全绿**（见 §6.5），当前 46 个文件中 45 全绿 + 1 无断言走查。

全量回归新发现并**已修复**（均为纯探针问题，零业务代码改动）：

| 探针 | 问题 | 修复 |
|---|---|---|
| probe-npc-schedule.mjs | 硬编码 `localhost:5178`（dev 实际在 5173）→ ERR_CONNECTION_REFUSED | GAME_URL → 5173 |
| probe-fix-mobile-hint.mjs | puppeteer `isMobile` 不改 UA（实测仍 Windows 桌面 UA），`isTouchDevice()` 走桌面分支 → 恒显桌面文案，断言必败 | 注入移动 UA（evaluateOnNewDocument，与 probe-mobile-layout 同法） |
| probe-density-v053-batch2.mjs（E6） | 假设"mystery 07:00 在 farm"，实际日程 06:00 森林 → 08:00 隐藏 → 16:00 森林（NPCSystem L115-122），**从未在 farm** | E6 主段 + 对照段改 forest 场景 |
| probe-claim-reward.mjs | **waitFor 定义 `(fn,timeout)` 与调用 `waitFor(page,fn,t)` 不匹配** → `timeout` 位收到函数，循环条件 `Date.now()-t0 < fn` = NaN → 立即返回 null，面板等待形同虚设（实测 DOM 两个面板 id 均存在） | waitFor 定义改 `(_page, fn, timeout)` |

**登记待重写（受并发修改者土地美术改造影响，非探针逻辑错误）——已于 2026-08-03 晚重写完成，见 §6.5**：

| 探针 | 原因 | 处理 |
|---|---|---|
| probe-automation.mjs | `TileVisual` 已从 `{rect,crop}` 改为 `{plot,crop}`（Image 瓦片，MapScene L90-93），探针访问 `.rect.visible` / `.rect.fillColor`（土地颜色反馈）失效 | ✅ 已重写：plot 可见性 + 帧号断言（tilled=帧0 / watered=帧2），giveRobot 流程核对保留，14/14 |
| probe-farm-tap.mjs | 同上结构失效；且 `tileVisual()` 把"视觉不存在"（exists:false）经 `before.visible`（undefined，falsy）误判为"格子隐藏 ✅"——**假绿** | ✅ 已重写：改为 `v.plot` 校验（exists 判空 + visible + frame），假绿修正（exists:false 直接返回不再判过），5/5 |

> 回归期间并发修改者在改 MapScene/EndingPanel/QuestPanel/GuiXingRecordSystem，Vite HMR 会整页 reload 打断探针页面（navigation destroyed）。已隔离确认：**环境噪声非探针问题**，避开 src 修改窗口重跑即过。本轮 4 个修复探针均在稳定窗口复跑通过。

## 六、三端测试记录（2026-08-03）

按制作人指令"移动端 / 安卓端 / 网页端 全部测试"，实际执行：

### 6.1 移动端网页（Puppeteer 移动 UA + 竖/横屏视口，全部 ✅）

| 探针 | 覆盖 | 结果 |
|---|---|---|
| probe-mobile-ux | 触屏背包按钮/关闭/横屏 | 8/8 |
| probe-mobile-layout | 触屏任务面板左上、竖屏、桌面右上 | 11/11 |
| probe-mobile-text | 摇杆/点按文案适配 | 6/6 |
| probe-mobile-tutorial | 触屏交互键驱动完整教程 | 10 步全过 |
| probe-mobile-sleep | 木屋/屋内/床边 3 路径睡觉 | 全过 |
| probe-fix-mobile-hint | 横屏文案 + 提示条清理 | 5/5 |
| probe-rotate-hint | 竖屏显示/横屏隐藏/桌面不显示 | 3/3 |
| probe-quest-btn-topleft | 横竖屏按钮可视区域 | 10/10 |
| probe-bug030-034 | 桌面隐藏 / Android UA 显示 / 按钮定位 | 13/13 |
| probe-bug031 / bug032 | 任务面板避让挖孔 / 屋外不误睡 | 2/2 + 4/4 |

### 6.2 网页端全量复核（43 全绿中的本轮复跑项，全部 ✅）

核心流程组：test-tutorial、test-woodcutting、probe-sleep(-realpath)、probe-farm-restore(25)、probe-map-richer(28)、probe-layout(17)、probe-farm-visual(21)、probe-forest-visual(3)、probe-npc-schedule(7)、probe-npc-daily-action(8)、probe-guixing-record(17)、probe-ambience(9)、probe-ambience-longrun(3)、probe-clear-save(6)、probe-bug012(13)、probe-bug035(6)、probe-water-splash(5)、probe-guide-dialogue(6)、probe-note-vs-woodcut(2)、probe-elder-portrait(5)、probe-title-avatar(4)、test-ch1-story(24)、probe-npc-life(8)、probe-b1(13)、probe-density-v053(9)、probe-density-v053-batch2(10)、probe-first-hour(15)、probe-garden-xiya(10)、probe-claim-reward(4)。

### 6.3 本轮新发现并修复（2 个，均为探针假设过时/时序问题，零业务代码改动）

| 探针 | 现象 | 根因 | 修复 | 复跑 |
|---|---|---|---|---|
| probe-stargaze.mjs | 三选项/分支B/结算面板/存档 6 项级联失败 | 未提交改动给 DEMO_ENDING_DIALOGUE 增补 3 行（信 1 + 林澈 2）→ 15 行变 17 文本行 + 选项 index=17；探针仍按 15 行 `skipDialogue(13)` → 选项行未到达 | `skipDialogue(13→16)` + 注释更新 | 13/13 ✅ |
| test-stress-switch.mjs | R2 挖矿 6→6、R3 切图失败级联 | ① `consumeMineTip` 在玩家未靠近矿脉时按 E，引导对话（要求近矿脉）未触发；7 次固定 advance 空转 → 引导对话延后到 mineOneOre 首次开采才弹，拦截开采；② 对话未关阻塞 `update()` 出口检测 → 切图失败 | 先瞬移到第一块矿脉旁再按 E；改为等对话打开后循环 advance 至关闭 | 26/26 ✅ |

> 两处修复印证同一原则：**探针必须实测可信**。行数/时序类假设易被剧情增补破坏，本轮无业务代码改动。

### 6.4 安卓端（无线连接实测 ✅，2026-08-03）

- **无线连接**：adb 37.0.1 mDNS 自动发现设备 `192.168.31.229:38795`（小米 23116PN5BC / Android 16 / 1440x3200 竖屏），`adb connect` 免配对直连成功
- **安装**：`tools/install_apk.py --variant release` 命中 release APK（21.8MB）→ 卸载旧版 → 安装 Success → 启动 MainActivity → 判活 OK
- **冒烟**：进程存活（pidof 有值）、crash buffer 无崩溃、screencap 截图解析非黑屏（147 唯一色 / 亮色点 738 / 暗色点 125，3200x1440 横屏渲染正常）
- 结论：**安卓端启动链路 + 首屏渲染通过**，无黑屏无崩溃；深度的触控/流程体验可随时在真机人工试玩

### 6.5 土地美术改造后探针重写记录（2026-08-03 晚）

> 背景：并发土地美术改造将 `TileVisual` 从 `{rect,crop}`（Graphics 深棕方块）改为 `{plot,crop}`（`farm_plot` 五态 Image 瓦片，帧：tilled=0/planted=1/watered=2/growing=3/grown=4），此前登记的 2 条待重写探针在此窗口一次性完成。

| 探针 | 重写内容 | 复跑 |
|---|---|---|
| probe-automation.mjs | ① 农田格渲染断言 `t.rect.visible` → `t.plot.visible`；② 浇水断言 `t.rect.fillColor(0x3d2817)` → `plot.frame===2` + `crop.frame===1`；③ 收获后 tilled 断言 `fillColor(0x6b4423)` → `plot.visible && plot.frame===0`；④ 清理死代码 `hex()` / itemCount 段 | 14/14 ✅ |
| probe-farm-tap.mjs | `tileVisual()` 改读 `v.plot`（`{exists, visible, frame}`）；假绿修正：`!v?.plot` 时返回 `{exists:false}`，`tilled` 判定改为 `exists && visible && frame===0`，不再把 undefined 判过 | 5/5 ✅ |

> 经验沉淀：TileVisual 是 **Phaser 渲染结构**，探针断言"玩家看到什么"应改为**语义状态**（帧号/可见性）而非内部色值——色值断言在美术迭代中极易碎。两条探针现已符合此原则。

### 6.6 排期建议执行：剩余探针缺口补齐（2026-08-03 晚 · trae）

> 依据：[v0.6剩余事项排期建议.md](v0.6剩余事项排期建议.md) A5/A6——制作人转任务卡后 trae 立即执行。

| 探针 | 缺口 | 补法 | 复跑 |
|---|---|---|---|
| probe-density-experience-v053.mjs | **无断言走查**（唯一 exit 0 不校验的探针，A5 假绿风险） | 加 `check()` 框架 + 6 断言：三场景进入（farm/town/mine）+ E1 清晨夏雅对话锚点「清晨的庄园很安静」+ E4 小镇闲聊触发 + E3 老张闲聊触发；失败设 exitCode=1 | 6/6 ✅ |
| probe-stargaze.mjs | **归星记录内容仅纯函数验证**，未断言真实面板内容（A6） | 面板打开后补断言：`#gx-sections` 五段渲染（count≥5）+ `#gx-header` 含「归星记录」+ `#gx-stats` 脚注存在 | 14/14 ✅（原 13 + 新 1） |

> 至此：46 个探针文件中**无断言走查归零**；观星结算 → 复苏报告面板真实触发链路（面板打开→内容渲染→存档→关闭自由游玩）全部真实断言。

