# 收口清单 — 2026-08-15（种植生活化 / 钓鱼生态化 / 邮箱系统 / 房子建模 / 出口修复批次）

> 目的：给 opencode / 制作人统一提交用的精确清单（提交仍由 opencode / 制作人执行，Agent 不执行 git 写操作）
> 状态：📝 今日收工前整理；工作区无 index.lock、无活跃 git 进程
> 基线：HEAD `269b1cf`（并行批次：采集流向扩展 / 树视觉升级 / 居民需求系统升级）

---

## 一、收（本批提交，逐文件 add）

| 文件 | 内容 |
|---|---|
| `src/scenes/MapScene.ts` | 种植切片 B 番茄架；图鉴墙美术（双柱展示架+缩小+移花田西南侧）；放生彩蛋+小鱼苗低概率事件；特殊鱼（河鳗/鲤鱼/大青鱼）+老姜台词定稿；土地回应系统 v1.4（蝴蝶蜜蜂+夏雅/小梅反馈）；邮箱系统（解锁/首封演出/来信队列/面板）；房子 2D 门脸建模（两态）+阴影调淡 |
| `src/data/exits.ts` | BUG 修复：farm→town 无限回跳（town 侧出生点 (7,18) 落在反向触发区 → 东移 2 格 (9,18)） |
| `src/data/FarmState.ts` | `countGrownTiles()`（土地回应触发判定）+ 移除老屋地板内两棵树 (3,20)/(8,21)（堵路） |
| `src/data/Inventory.ts` | 青禾鱼苗（不可售收藏）+ 河鳗/鲤鱼/大青鱼条目 |
| `src/data/Economy.ts` | 特殊鱼收购价 60/35/90G |
| `src/data/MailLetters.ts`（新增） | 邮箱定稿信件数据（爷爷首封 + 5 世界信 + 8 生活信） |
| `src/ui/MailboxPanel.ts`（新增） | 邮箱面板（列表/信件/已读归档）+ 首封爷爷演出 |
| `src/systems/NPCSystem.ts` | 小梅「最近菜园看起来不错」生活线（农田有成熟作物时 seed 轮换） |
| `public/assets/maps/farm.json` | 房子通路：移除南/东/西三面 03 墙，保留北面门洞；恢复误删 col8 地板 |
| `tests/probes/probe-fishing-phase1.mjs` | 钓鱼 T8 适配新鱼种（鲤鱼/大青鱼/河鳗时段 + 售价断言） |

## 二、不收 / 待定（明确排除或由收口方决定）

- `.tmp/` 下今日新探针：`probe-crop-trellis.mjs` / `probe-crop-field-life.mjs` / `probe-fish-release.mjs` / `probe-mailbox.mjs` / `probe-farm-town-exit.mjs` —— 按惯例临时探针不提交；如需纳入正式探针池，先移入 `tests/probes/` 再收。
- 诊断截图 / `test-results/` / `tests/probes/test-screenshots/cap-4*~cap-5*` —— 生成物，不提交。
- 并行 Agent 在途改动（工作区还有其他未提交文件，如 `probe-crop-visual-baseline.mjs` 等）——不在本清单，由收口方与并行方核对后另行处理。

## 三、验证状态（本批）

- `npx tsc --noEmit` ✅
- `.tmp/probe-crop-trellis.mjs` 9/9、`.tmp/probe-crop-field-life.mjs` 8/8、`.tmp/probe-fish-release.mjs` 10/10、`.tmp/probe-mailbox.mjs` 9/9、`.tmp/probe-farm-town-exit.mjs` 5/5
- 回归：`.tmp/probe-crop-gift.mjs` 10/10（切片 A）；`probe-fishing-phase1.mjs` 31/1（唯一失败=交接清单已知过时断言）
- 房子通路：移动实测（玩家从南侧走进房区 79px）

## 四、提交注意事项（Git 纪律）

1. **逐个 `git add <路径>`**，禁止 `git add -A` / `git add .`（工作区有并行在途改动，宽泛 add 会卷入他人文件）。
2. 提交前确认：无活跃 git 进程、无 `.git/index.lock`。
3. 高危命令（reset/checkout/clean/restore）禁止；如需回退用 `git revert`。
4. 建议提交后打 tag 作恢复锚点（如 `v0.7-crop-fish-mail-20260815`）。

---

## 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-08-15 | v0.1 | 收工前整理：种植/钓鱼/邮箱/房子/出口修复批次收口清单 |
