# 心语·二《花期未至》9 段 stage 机·兼容边界核对（2026-08-29）

> 性质：**施工前核对（不改 code、不改正文）**｜目的：把"春祭/烟花 → 秋日晒场"只做台词语义替换的前提下，锁定哪些结构/门禁/语义不可碰
> 依据：MapScene.ts `setupBloomXiya / tryBloomXiyaInteract / saveBloomFlags` 等（L16646-16880）＋StorySystem.ts `XIYA_BLOOM_*_DIALOGUE`（L819-971）＋制作人 2026-08-29 拍板（口径＝秋日晒场）

---

## 一、状态机总表（现存代码事实）

| code stage | 段 | 交互对象 | 对白常量 | 交互点标签（bloomMarkTextForStage） | 推进 |
|---|---|---|---|---|---|
| 0 | **S1 承接开场** | 夏雅 spawn（公告栏旁） | `XLYA_BLOOM_S1_OPEN_DIALOGUE` | （夏雅本体） | 0→1 |
| 1 | **S2 仓库整理** | 标签 | `…S2_STORAGE` | 「旧布匹」 | 1→2 |
| 2 | **S3 修花架** | 标签 | `…S3_FLOWERSTAND` | 「花台材料」 | 2→3 |
| 3 | **S4 误解形成**（名单/爷爷名字） | 标签 | `…S4_MISUNDERSTAND` | 「邻居婆婆」 | 3→4 |
| 4 | **S5 真相转换**（村民加入） | 标签 | `…S5_TURNING` | 「日记纸页」 | 4→5 |
| 5 | **S6 村民参与** | 标签 | `…S6_VILLAGERS` | 「邻居们」 | 5→6 |
| 6 | **S7 春祭日/不完美的傍晚** | 标签 | `…S7_FESTIVAL` | 「春祭摊位」 | 6→7 |
| 7 | **S8 收束演出** | 标签 | `…S8_FIREWORKS` | 「烟花灯」 | 7→8 |
| 8 | **尾声（最终收束点）** | 夏雅 re-spawn | `XLYA_BLOOM_EPILOGUE` | （夏雅本体） | 8→9(done) |

通用推进 `advanceStage(next, dialogue, after?)`：ensureMusic(spring_letter) → asked=true → stage=next → `saveBloomFlags()` → play → 清理当前对象 → save → 按新 stage 生成下一对象。

## 二、不可碰清单（正文/实现替换时必须原样保留）

| # | 项 | 现状语义 |
|---|---|---|
| 1 | **段落数量与顺序** | S1–S8＋尾声＝code stage 0→8→9；与正文 S1-S8＋收尾一一对应。**不增删段落、不改顺序**；正文各段只允许改台词措辞 |
| 2 | **前置门禁 `bloomPrereqOk`** | `xiyaLetterDone(·一完成) && isRestored('marketSquare')`——·一 → 集市恢复 → 花期未至 的链不变 |
| 3 | **时段门禁 `bloomTimeOk`** | 8≤hour<20；跨日读档保持（stage 已入档） |
| 4 | **存档语义** | `xiyaBloomAsked/Done/Stage`（均 optional，旧档兼容，不升 SAVE_VERSION）；`saveBloomFlags()`＝`saveAtPlayer()`（x/y/scene/facing/dailyQuest 同 ·一 契约） |
| 5 | **尾声＝最终收束点** | stage8→9：done=true → `playStory(EPILOGUE)` → `clearBloomXiya` → `spawnBloomPermVignette`（常驻小景）→ `showStoryComplete` → save → restoreBgm。**不被任何"晒场"逻辑前置/取代** |
| 6 | **交互路由协议** | `tryBloomXiyaInteract` 的 R=32px 判定、按 stage 分派、advanceStage 通用推进——不动 |
| 7 | **专属音乐** | `spring_letter`（标题《花期未至》保留，音乐不动） |
| 8 | **核心语义句（一字不动）** | S3「有人记得，它就还是这个岛的东西。没人记得……它就只是一块木头了。」／S5「我只是觉得……明年，可能就真的没人记得了。」「我等的，是有人还愿意一起做这些事。」「以前回不来。只是……如果没有人接着做，它就一直留在"以前"了。」／S6「不是我拉他们回来，是日子把他们带回来了。」／S8「现在，你看，大家都在看。」／尾声「我只是，不想让它被我忘了。」 |

## 三、可改清单（∎ 只改台词语义，不新增状态机节点）

正文（`夏雅角色篇章-春深有信-v1.0.md`《花期未至》）与 code 台词常量（`XLYA_BLOOM_*`）**同源双写，两处同步替换**：

| 位置 | 现措辞（春祭/烟花系） | 校准方向（秋日晒场系） |
|---|---|---|
| S1 | "摊子还没支起来" | 晒架还没支起来 |
| S2 | 花架×2／"还能再站一个春天"／"纸花" | 晒架／"还能再撑一个秋天"／干菜·竹席 |
| S3 | 花架×5／"每个春天" | 晒架／"每年这个时候" |
| S4 | "春祭"×2／名单「花苗·点心·场地·烟花」／"原来烟花以前也是会放的" | 秋日晒场／名单「玉米·鱼干·场地·灯笼」／"原来灯笼以前也是要挂满的" |
| S5 | "春祭前一天"／王婶"弄春祭" | 晒场前一天／"弄晒场" |
| S6 | "春祭当天早晨"／"老花架" | 晒场当天／"老晒架" |
| S7 | "纸花褪了色" | "竹席补过、玉米串得不太齐" |
| S8 | "烟花"×5（搬箱/升空/没人放/看/一次次升空）＋高潮语义 | 收束演出＝人回来了＋远处灯塔亮一下（**对齐秋日晒场定稿，非新节点**） |
| 尾声 | "纸花"／"樱花落地还会再开" | 环境词换晒场系；隐喻句保留或换晒场隐喻（**待制作人定**） |
| 标签 | 「春祭摊位」「烟花灯」 | 「晒场摊位（或晒架）」「灯笼」（**待定稿**） |
| 完成文案 | 「明年春天，我们再一起等一场花会。」 | 按秋日晒场口径（**待定稿**，候选「明年这时候，我们再一起晒一场。」） |
| 永久小景 | 纸花＋挂饰＋暖灯（"春祭记忆小景"） | 视觉内容改晒场系（竹席/干菜/灯笼），**不做新状态**（**待定稿**） |

## 四、核对结论

1. ✅ **结构边界安全**：段落 8+1、顺序、`bloomPrereqOk`/`bloomTimeOk`、`saveBloomFlags`/`xiyaBloomDone` 语义、尾声收束点——全部不依赖具体活动名，替换不触碰。
2. ✅ **替换面纯属台词层**：正文文档＋code `XLYA_BLOOM_*` 常量＋标签＋完成文案＋小景视觉，均为"文本/视觉内容"，不新增状态机节点、不动 stage 编号。
3. ⚠️ **三处待制作人定稿**（进入①前需拍板）：① 尾声隐喻句"樱花落地还会再开"是否保留（花语隐喻 vs 晒场隐喻）；② 完成文案（"明年春天…花会"→？）；③ 永久小景视觉语义（纸花挂饰→晒场系）。其余全部可直接按 §三 逐字替换。
4. ✅ **探针回归面**：现有 bloom 相关探针（若有）与 `window.__game` 反射断言不读台词文本（stage/事件/存档为取证源）时不受影响；若有断言读台词，替换后同步（① 阶段核）。

## 五、下一步（① 阶段）
制作人确认 §三/§四 待定 3 项后：
1. 替换正文（`夏雅角色篇章-春深有信-v1.0.md` S1-S8+尾声）＝适用已交付校准稿；
2. 同步替换 code 台词常量（`XLYA_BLOOM_*`）＋标签＋完成文案＋小景视觉；
3. probe 回归（含台词断言同步）。

## 附：引用
MapScene.ts L16646-16880（D-011 花期未至专线）｜StorySystem.ts L815-971（XLYA_BLOOM_* 九段台词）｜`心语二《花期未至》秋日晒场口径校准稿-v1.0-2026-08-29.md` ｜秋日晒场设计定稿 v1.1