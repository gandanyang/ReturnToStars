# P1 交付后→观星引导：最小修改方案

> 🟢 **方案有效 / 尚未施工**（2026-08-13 核对标记）：观星夜仍是第0章核心收束节点（D-021），本方案不过期；但「夜晚的庄园」引导行**经核对未落地**（`SHARD_DELIVER_DIALOGUE` 仍 8 行，`src/systems/StorySystem.ts` L281-293 无该行）——仍是待办，非当前批次优先项（当前焦点=三阶段体验链 + 心语任务收尾）。
> 状态：📋 已拍板（制作人 2026-08-04：P1 实施 / P2 暂缓）→ **待施工**（opencode 领地 StorySystem.ts / trae 同步探针）
> 依据：《第一章体验分析》§1 观察点（`6893af0`）+《问题追踪.md》观察项
> 性质：体验引导补全，**不改主线剧情** / **零新系统 / 零存档字段**
> 施工归属：`StorySystem.ts` = opencode 领地；探针同步 = trae

---

## 1. 问题

交付对白（SHARD_DELIVER_DIALOGUE）末尾只有「主线任务完成：星之碎片（1/…）。」，观星点仅在 farm + 夜晚 20:00 后 + 主线完成时显示。**玩家白天交付后不知道"夜晚回庄园"有收束剧情，可能交付后结束游戏，Demo 结局（观星夜 + 结算面板）被错过。**

## 2. 修改内容（1 处，+1 行）

**文件**：`src/systems/StorySystem.ts`

**插入点**：`SHARD_DELIVER_DIALOGUE` 第 8 行「主线任务完成：星之碎片（1/…）。」**之前**（即镇长情感落点之后、系统确认行之前，保持"情感 → 引导 → 系统确认收尾"的既有节奏，与序章每段对白末尾提示模式一致）。

**新增行**（system 色旁白，非角色台词，不剧透观星机制）：

```ts
{ speaker: '', color: COLORS.system, text: '夜晚的庄园，似乎有什么在等着你。' },
```

> 文案采用制作人拍板时确认的建议文案（《第一章体验分析》§1 缓解方向）。如需微调请制作人圈定，施工方按定稿实现。

**行数变化**：SHARD_DELIVER_DIALOGUE 8 → 9 行。

## 3. 影响面与同步项

| 文件 | 现状 | 需同步 |
|------|------|--------|
| `tests/probes/test-ch1-story.mjs` L257 | `skipDialogue(page, 8)` | 8 → **9**（trae） |
| `tests/probes/probe-stargaze.mjs` L193 | `skipDialogue(page, 8)` | 8 → **9**（trae） |
| `tests/probes/probe-ch1-walkthrough.mjs` | walkDialogue 动态读行 | 无需同步（trae 重跑验证提示行出现） |

**不涉及**：存档结构 / 其他对白 / 其他系统。移动端探针不受影响（不覆盖交付段）。

## 4. 验证清单（trae 执行，opencode 施工落地后）

1. `tsc` 0 错
2. `test-ch1-story.mjs` 全绿（交付段 9 行推进正确，questState=completed）
3. `probe-stargaze.mjs` 全绿（交付 9 行 + 观星全流程）
4. `probe-ch1-walkthrough.mjs` 重跑：确认「夜晚的庄园，似乎有什么在等着你。」出现在交付对白记录中，且 0 运行时错误
5. 回归抽查：probe-farm-restore 25/25（farm 相关，确保无波及）

## 5. 协作分工

1. **opencode**：施工 `StorySystem.ts`（+1 行）
2. **trae**：同步两处探针行数（8→9）→ 跑验证清单 → 验收落档（问题追踪.md）
