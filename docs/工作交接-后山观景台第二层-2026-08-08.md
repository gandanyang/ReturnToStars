# 工作交接：后山观景台（星之碎片第二层）— 未完成

> 交接时间：2026-08-08 04:50（会话切换）
> 续作前必读：`docs/AI开发前必读.md` + `docs/开发约束与架构入口.md` + `.workbuddy/memory/2026-08-08.md`

## 一、背景：制作人拍板的三层方案

制作人（2026-08-08 凌晨）提供"星之碎片发现事件应增加**意义密度**"分析，核心结论：碎片从"推动剧情的道具"升级为"改变林澈认知的事件"，主线 20 分钟 → 30 分钟，但**不堆任务、不推翻设定**。

- **第一层（纯对白）✅ 已完成**：见下方"已完成"节
- **第二层（后山环境物 + 夏雅生活记忆对白）🔴 进行中**：本次交接对象
- **第三层（三节点扩充：老屋整理 / 后山主动探索 / 碎片后岛民反应）⏸ 未排期**：需制作人拍板 Demo 范围

## 二、已完成（勿重做）

### 第一层（纯对白，系统/存档/任务结构零改动，已验证）
- `src/systems/StorySystem.ts`
  - `FOREST_SHARD_DIALOGUE` 9→14 行：环境铺垫（青苔石阶/老树旁）→ 夏雅"我以前从没见过"（碎片=新变化）→ 林澈工程师视角保留（"更像一个长期没有维护的系统"，探针断言此句）→ **新增情感句"我以为回来以后，只会看到一座快要消失的岛"** → girl"它沉睡太久了"
  - `SHARD_DELIVER_DIALOGUE` 10→13 行：村长补"不是留下来的，是等着被发现的" + "这么多年过去，终于又有人走到这里来了"
- **探针修复**：`tests/probes/probe-stargaze.mjs` 在 f7（08-07 村长第一天忙线）后一直坏——补 `debug.nextDay()`（day=2）+ 台词行数同步（forest 14 行 / 交付 13+7=20 行含 WHY_FARM）。**现 14/14 全绿**
- 验证：tsc ✅ + probe-stargaze 全绿

### 第二层已完成部分
- `src/systems/StorySystem.ts` 行 263：**`FOREST_LOOKOUT_DIALOGUE` 已加**（夏雅"……这里以前有个观景台。"等，一次性环境铺垫对白）
- `src/scenes/MapScene.ts` 行 88：**import 已加**（`FOREST_LOOKOUT_DIALOGUE`）

## 三、未完成（本次交接任务）

### ⚠️ 最高优先级：编译风险
`tsconfig.json` 开了 `noUnusedLocals: true` → **MapScene.ts:88 的 `FOREST_LOOKOUT_DIALOGUE` import 已加但未使用，当前 `tsc --noEmit` 会报错（TS6133）**。续作第一步必须把它用起来（完成下方实现）或临时处理，否则构建挂。

> ✅ **2026-08-08 07:00 已全部完成**（见文档末尾"完成记录"）。以下"待实现"仅为历史记录，勿重做。

### 待实现（按此顺序）
1. **画观景台**：`src/scenes/MapScene.ts` 字段区（约 432 行附近，上次正读到此处）声明观景台 sprite/graphics 字段；在 forest 场景绘制。布局参考：forest 地图碎片在 (20,10)，观景台放碎片上方 **(19-21, 6-8)** 空地（玩家走向碎片时先经过）。
2. **靠近触发一次性对白**：玩家走到观景台附近 → 播放 `FOREST_LOOKOUT_DIALOGUE`，只播一次。
   - **模板**：`MapScene.ts` 约 1919 行"阿风欢迎演出"的靠近触发检测（现成范式，复制即可）
   - **判重**：`EventManager.triggerOnce(id, fn)` / `hasTriggered(id)`（src/systems/EventManager.ts），事件 id 建议 `forest_lookout_first_visit` 之类
   - 注意：观景台对白**不改变**碎片发现流程，`FOREST_SHARD_DIALOGUE` 仍正常触发
3. **验证**：tsc --noEmit + probe-stargaze（预期 14/14 仍绿；若观景台触发加进流程探针，需同步——建议不加，探针走直线路径）

## 四、世界观适配红线（制作人已确认）

- 建议里的"看海"→ 一律改**星空意象**（观星夜/爷爷看天闭环，不另开海线）
- 禁止推翻现有设定；不新增系统/存档字段/任务结构；EventManager 不扩接口，只做消费方
- 新台词最终走 **MiniMax 配音管线**（文案定稿 → 配音 → 接入），当前只做文案落地

## 五、后续待拍板清单

1. 第一层/第二层新台词文案制作人过目 → MiniMax 配音（新增：夏雅/林澈/村长 约 5 条）
2. 第三层（三节点扩充，Demo 拉 30 分钟）是否排期 → 建议单独立任务卡
3. 美术：gate tileset 升级（P1 最后一步）仍挂着；车站/星空后续
4. ⚠️ 工作区大量改动**未 commit**（多 AI 协作，提交前确认无并发 git）

## 六、相关文件索引

| 文件 | 位置 |
|---|---|
| 观景台对白数据 | `src/systems/StorySystem.ts:263` |

## 七、完成记录（2026-08-08 07:00）

- **实现**：MapScene.ts `setupForestLookout()`（forest (20,7) 废弃木台：阴影/发黑木板/立柱顶梁/塌围栏/苔藓，depth 5）+ `checkForestLookout()`（update 每帧 dist<70 → triggerOnce('forest_lookout_first_visit') 播 6 行对白 → save，阿风模板）。⚠️ 交接时记录的"import 已加"实际已不在（另一会话回退），从零加回。
- **探针同步**：probe-stargaze 加观景台步骤（waitAndSkipDialogue 6 行）；probe-ch1-walkthrough 加观景台步骤 + **补 `debug.nextDay()`**（f7 后村长忙线导致碎片不出现的探针过期，与 stargaze 同步修复）。
- **验证**：tsc ✅ / probe-stargaze 14/14 ✅ / probe-density-v053-batch2 10/10 ✅（E2 失败为 dev server 重启后首编慢的时序 flake，重跑即过）/ probe-ch1-walkthrough 0 错误 ✅（观景台 6 行 + 碎片 14 行 + 闪回完整）。
| MapScene import | `src/scenes/MapScene.ts:88` |
| 字段声明区（下一步改这） | `src/scenes/MapScene.ts` ~432 |
| 靠近触发模板（阿风） | `src/scenes/MapScene.ts` ~1919 |
| 探针 | `tests/probes/probe-stargaze.mjs` |
| 记忆日志 | `.workbuddy/memory/2026-08-08.md` |
