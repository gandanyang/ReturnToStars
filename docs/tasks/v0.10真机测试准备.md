# v0.10 真机测试准备

> 制作人执行前参考｜2026-08-06（19:31 更新）

---

## 测试前准备

### 1. 打包 APK（本次已完成）

```bash
# 设置 JAVA_HOME（JDK 21）
set JAVA_HOME=C:\Java\jdk-21.0.12+8

# 打包 debug APK
python tools/build_apk.py --variant debug
```

**本次产物（2026-08-06 19:31 打包，已校验）**：
- `android/app/build/outputs/apk/debug/app-debug.apk`（307.9 MB）
- 副本：`dist_apk/latest-debug.apk`
- 内容：含 VIS-02 镇长家/小镇视觉升级 + 智能出售系统（SmartSellPreviewPanel）+ 恢复后的旧版主线配音（41 条错误重录未进包）

### 2. 安装到手机

```bash
adb install android/app/build/outputs/apk/debug/app-debug.apk
```

adb 位置：`C:\Users\Gdy\AppData\Local\Android\Sdk\platform-tools\adb.exe`（不在 PATH，需全路径或先装 platform-tools 到 PATH）

---

## 测试清单

### A. 音频验证（P0）

| # | 测试项 | 预期结果 | 方法 |
|---|--------|----------|------|
| A1 | 标题页 BGM | 播放正常，无 IDM 下载弹窗 | 进入标题页，听音乐 |
| A2 | 农场白天 BGM | 进入农场播放 farm_day | 进入农场，白天 |
| A3 | 农场夜晚 BGM | 19:00 后切换 stargaze_night | 睡觉跨天到晚上 |
| A4 | 观星夜 BGM | 观星剧情播放 stargaze_final | 主线完成后触发观星 |
| A5 | 场景切换无叠播 | 切场景后旧音乐停止 | 来回切换场景 |
| A6 | 主线配音 | 140 条归一化配音播放正常（**当前包为旧版配音**，重录 41 条未进包，语音任务受阻） | 走序章+第一章流程 |
| A7 | 记忆闪回演出 | 森林采集碎片后出现童年记忆闪回（点击逐行推进，播完自动采集） | 走主线森林采集段，听闪回音效/观演出 |

### B. 存档验证（P0）

| # | 测试项 | 预期结果 | 方法 |
|---|--------|----------|------|
| B1 | 自动存档 | 睡觉后存档成功 | 睡觉→杀进程→重开 |
| B2 | 存档恢复 | 位置/背包/任务恢复 | 重开后检查状态 |
| B3 | 跨天存档 | 日期/作物进度恢复 | 睡觉跨天→重开 |
| B4 | 删档 | 清除存档后重新开始 | 标题页删档→重开 |

### C. 触摸验证（P0）

| # | 测试项 | 预期结果 | 方法 |
|---|--------|----------|------|
| C1 | 摇杆移动 | 8方向移动正常 | 触屏操作 |
| C2 | 交互按钮 | 靠近交互物按 E 触发 | 靠近NPC/物品 |
| C3 | 背包按钮 | 打开/关闭背包 | 按 B 按钮 |
| C4 | 任务按钮 | 打开/关闭任务 | 按 J 按钮 |
| C5 | 种植操作 | 锄地/播种/浇水/收获 | 触屏操作农田 |

### D. 返回键验证（P0）

| # | 测试项 | 预期结果 | 方法 |
|---|--------|----------|------|
| D1 | 对话中返回 | 不跳过对话 | 对话中按返回 |
| D2 | 游戏中返回 | 弹出退出确认 | 游戏中按返回 |
| D3 | 退出确认 | 继续/回主菜单/退出 | 点击选项 |

### E. 性能验证（P1）

| # | 测试项 | 预期结果 | 方法 |
|---|--------|----------|------|
| E1 | 帧率 | 稳定 30+ fps | 观察画面流畅度 |
| E2 | 内存 | 无明显泄漏 | 玩 10 分钟后检查 |
| E3 | 加载速度 | 场景切换 <2s | 计时 |

---

## 已知问题（可绕过）

| 项 | 说明 | 影响 |
|----|------|------|
| BUG-047 | IDM 下载弹窗 | 已改为 Web Audio，需桌面验证 |
| 夏雅语音重录 | 41 条已生成但判定错误，voice_normalized 已恢复旧版 | 真机听到的是旧版配音；重录任务受阻待制作人确认 |
| 语音串词 | station_04/garden_01/02 待重录 | 部分台词语音不匹配（旧版） |
| 音量差异 | 部分配音偏大/偏小 | 响度已归一化，但仍有差异 |
| TEST-02 | 3 个主线探针未适配闪回系统（浏览器侧），功能已验证正常 | 不影响真机，真人操作链路通 |

---

## 验证后反馈

请记录以下信息：

1. **音频**：BGM 播放是否正常？有无下载弹窗？
2. **存档**：存档/恢复是否正常？有无坏档？
3. **触摸**：操作是否流畅？有无卡顿？
4. **返回键**：退出确认是否正常？
5. **性能**：帧率如何？有无卡顿？

---

*文档生成时间：2026-08-06*
