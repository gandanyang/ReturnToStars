# 任务卡：v0.6 视觉升级（第一批）

> 指派对象：opencode / trae（美术开发）
> 发起依据：[顶层设计.md §10.6 决策②③](../../顶层设计.md) + [需求文档 §13.2](../../需求文档.md) + [美术规范.md](../../美术规范.md)
> 优先级：v0.6 第一批（制作人已拍板顺序）
> 状态：⏳ 草案，待 v0.6 启动 + 制作人确认后进入实现

---

## 0. 前置纪律（必读）

1. **不提前实施**：当前阶段为 v0.5.3 → v0.5.4 移动端体验，美术升级整体延后到 v0.6（[顶层设计 §10.6 决策②](../../顶层设计.md)）。本任务卡仅作排期准备，不得在 v0.5.x 阶段开工。
2. **顺序不可调**：[顶层设计 §10.6 决策③](../../顶层设计.md) 已拍板第一批顺序 = UI → 主角头像 → 夏雅立绘 → 第二批（镇长等）。Agent 不得自行调整。
3. **不新建管线**：所有资源由既有脚本生成（[美术规范 §5](../../美术规范.md)），扩展不新建。
4. **v2 像素风不可回退**：逐像素绘制、1px `C.OUTLINE = (18, 14, 22, 255)` 描边、三色调 + 高光（[美术规范 §1](../../美术规范.md)）。
5. **领地避让**：开工前先 `git status`，若其他 AI 在改 `src/scenes/MapScene.ts` / `src/ui/*` / `tools/gen_*.py`，等待协调。
6. **剧情权限**：本任务仅产出美术资源 + 接线，不扩写剧情、不改角色设定。

---

## 1. 任务目标

按 [顶层设计 §10.6 决策③](../../顶层设计.md) 第一批顺序，补齐 4 项视觉缺口：

```
1. UI 套件（HUD / 对话框 / 任务面板 / 背包面板）
2. 林澈头像
3. 夏雅立绘 + sprite 升级（覆盖三处形象）
4. 镇长立绘（暂缓一个迭代后接续）
```

**核心判据**（[顶层设计 §10.6 决策②](../../顶层设计.md)）：
> 立绘收益受剧情密度约束——"先认识她，再被她的美打动"。v0.5.3 剧情密度增强已落地，v0.6 启动美术升级的时机成立。

---

## 2. 现有资源盘点（v0.5.3 收尾状态）

| 类别 | 数量 | 状态 | 文件位置 |
|------|------|------|---------|
| 角色 sprite（32×32） | 7 | ✅ v2 像素风已统一 | `public/assets/sprites/` |
| 场景物件 sprite | 6 | ✅ v2 已重绘（tree1/tree2/stump/ore_*） | `public/assets/sprites/` |
| 物品图标（16×16） | 18 | ✅ 已通过 `itemIconHtml()` 接入 | `public/assets/icons/` |
| 立绘 portraits | 2 | ⚠️ 仅林澈 + 夏雅，镇长等无立绘 | `public/assets/portraits/` |
| 地图 tileset | 7 | ✅ | `public/assets/tiles/` |
| 封面 | 1 | ✅ title_bg.jpg | `public/assets/images/` |
| UI 套件 | ❌ | DOM + emoji 混搭（💠⚡🪓） | `src/ui/ui-theme.css` 仅按钮视觉 |

**已完成任务卡**：[任务-美术P0风格统一重绘.md](任务-美术P0风格统一重绘.md) ✅（6 张 v2 像素风重绘）

---

## 3. 逐项设计要求

### 3.1 UI 套件（优先级 1 — 玩家每分钟看到）

**现状**：HUD 用 emoji 文字指示（`💠` 钻石 / `⚡` 体力 / `🥕` 种子 / `🪓` 工具提示）；对话框/任务面板/背包面板为纯 DOM 文字 + `ui-theme.css` 仅做按钮渐变。

**目标**：统一视觉语言，替换 emoji 混搭为像素风图标 + 一致面板风格。

**子项**：

| 子项 | 涉及文件 | 要求 |
|------|---------|------|
| HUD 图标（钻石/体力/种子/金币） | `src/scenes/MapScene.ts` updateHUD | 16×16 像素图标替换 emoji；保留 `textContent` 文字数字（[美术规范 §8](../../美术规范.md) 允许） |
| 对话框样式 | `src/ui/StoryDialogue.ts` + `ui-theme.css` | 立绘框 + 文字框视觉统一；底部居中保持（移动端硬约束） |
| 任务面板 | `src/ui/QuestPanel.ts` | Drawer 式，分类 Tab；与背包/商店面板同一套色板 |
| 背包面板 | `src/ui/BackpackPanel.ts` | 物品图标网格；复用 `itemIconHtml()` |
| 商店面板 | `src/ui/ShopPanel.ts` | 与背包同风格 |

**调色板**：遵循 [移动端UI优化规划.md §4.4](../../移动端UI优化规划.md)（主色 #4caf50 / 次色 #2196f3 / 强调 #ffd700 / 警告 #ff5722 / 背景 rgba(25,20,15,0.92)），与 v2 像素风描边语言协调。

**禁止**：
- ❌ 改移动端对话框位置（硬约束：底部居中 `y = scale.height - 180` + `setScrollFactor(0)`）
- ❌ 改 HUD 信息分级（PC 完整 / 移动端精简两行）
- ❌ 引入新依赖（图标库等）

### 3.2 林澈头像（优先级 2 — 二游第一印象）

**现状**：立绘 `portraits/linchen.png` 已接入 StoryDialogue；无独立头像/icon。

**目标**：产出林澈头像，用于（待定）：标题界面 / 存档界面 / HUD 角色角标 / 对话框小头像。

**规格**（[美术规范 §2](../../美术规范.md) + [顶层设计 §8.5](../../顶层设计.md)）：
- 立绘头像框：桌面 128×128 / 移动端 96×96（已实现）
- 头像源图：建议 512×512 RGBA 透明背景，从现有 `linchen.png` 半身裁切或独立绘制
- 风格：与现有立绘一致，沿用 `tools/gen_portrait_final.py` 管线

**禁止**：
- ❌ 改林澈角色设定（27 岁程序员 / 内敛疲惫 / `#7eb8da`）
- ❌ 新建生成管线（扩展 `gen_portrait_final.py`）

### 3.3 夏雅立绘 + sprite 升级（优先级 3 — 第一核心角色）

**现状**：
- 立绘 `portraits/xiya.png` 已接入
- sprite `sprites/npc_xiya.png` 已 v2 重绘，但**形象不符人设**（[需求文档 §13.2](../../需求文档.md) 登记）

**目标**：升级 sprite 覆盖三处使用点，保持形象一致；立绘同步升级（如制作人确认）。

**sprite 三处覆盖**（[需求文档 §13.2](../../需求文档.md) + [维护说明 §9](../../维护说明.md)）：

| 位置 | 场景 | 当前显示 | 要求 |
|------|------|---------|------|
| ① gate 开门关 | `MapScene('gate')` | 32×32 未缩放（`add.sprite` 无 setScale） | 形象升级后保持 32×32 显示 |
| ② farm 教程 | `MapScene('farm')` | 夏雅教程引导 | 与①一致 |
| ③ E1 清晨偶遇 | `MapScene('farm')` `XIYA_DAWN_DIALOGUE` | 06:00-08:00 触发 | 与①②一致 |

**生成方式**：扩展 `tools/gen_style_unify.py`（[美术规范 §5](../../美术规范.md)），不新建管线。

**角色设定**（[剧情开发规划 §2.1](../../剧情开发规划.md)）：
- 夏雅，20 岁，镇长助理 / 机械维修师
- 对话色 `#f0a050`（橙金短发 / 服装点缀）
- 工装感（背带 / 工具腰带可选，呼应机械维修师身份）
- 声音特征：直率、温暖、带一点揶揄

**禁止**：
- ❌ 改夏雅角色设定（[剧情权限规则](../../AGENTS.md)）
- ❌ 改 gate 场景 `add.sprite` 未缩放设计（[美术规范 §8](../../美术规范.md) 既有设计，勿动）
- ❌ 新建生成脚本

### 3.4 镇长立绘（优先级 4 — 暂缓一个迭代后接续）

**现状**：无立绘，对话框用首字色块占位（色 `#c8b898`）。

**目标**：产出镇长立绘，接入 StoryDialogue `PORTRAIT_MAP`。

**规格**：同 §3.2（512×768 RGBA，`gen_portrait_final.py` 管线）。

**角色设定**（[剧情开发规划 §2.1](../../剧情开发规划.md)）：
- 镇长，爷爷的老友，世界观传达者
- 沉稳、怀念、长者语气
- 米黄色调

**启动时机**：[顶层设计 §10.4 附加决定](../../顶层设计.md) — "镇长立绘暂缓一个迭代"，即本批次 3.1-3.3 完成后再启动。

---

## 4. 实现方式建议

遵循 [美术规范 §5](../../美术规范.md) 与 [任务-美术P0风格统一重绘.md §4](任务-美术P0风格统一重绘.md) 模式：

1. **扩展既有脚本**，不新建管线：
   - UI 图标 → 扩展 `tools/gen_item_icons.py`
   - 林澈头像 / 镇长立绘 → 扩展 `tools/gen_portrait_final.py`
   - 夏雅 sprite → 扩展 `tools/gen_style_unify.py`
2. **脚本确定性**：固定种子 / 无 random 抖动，可重复运行输出一致
3. **接线**（美术产出后单独 commit）：
   - HUD 图标：`MapScene.ts` updateHUD 替换 emoji
   - 立绘：`StoryDialogue.ts` `PORTRAIT_MAP` 添加映射
   - sprite：`NPCSystem.ts` `textureKey` 不变（覆盖同名文件）

---

## 5. 边界与禁止事项

### ✅ 允许修改
- `public/assets/` 下对应 PNG 文件（覆盖）
- `public/assets/portraits/` 下立绘文件
- `tools/` 下既有生成脚本（扩展）
- `src/ui/*.ts` + `src/ui/ui-theme.css`（接线 UI 套件）
- `src/scenes/MapScene.ts` updateHUD（接线 HUD 图标，注意避让其他 AI 在途改动）

### ❌ 禁止修改
- `src/systems/StorySystem.ts`（剧情数据，不属本任务）
- `src/data/`（数据层，不属本任务）
- `public/assets/maps/` / `public/assets/tiles/`（场景瓦片不属本任务）
- `顶层设计.md` / `剧情开发规划.md` / `林澈圣经.md`（制作人领地）
- `任务-美术P0风格统一重绘.md`（已验收历史任务卡）

### ❌ 禁止行为
- 新建生成脚本（必须扩展既有）
- 回退 v2 像素风（逐像素 / 1px `C.OUTLINE` 描边 / 三色调）
- 改移动端硬约束（对话框位置 / HUD 分级 / `isMobileLayout()`）
- 改角色设定（年龄 / 身份 / 对话色 / 性格）
- 改存档结构（不新增字段 / 不升版本号）
- 并行实施 3.1-3.4（按顺序推进，单项验收后再下一项）

---

## 6. 验收标准（DoD）

### 6.1 通用项
- [ ] 所有新增/重绘资源为 v2 严格像素风（逐像素 / 1px 描边 / 三色调）
- [ ] 生成脚本可重复运行且输出一致（`git status` 干净）
- [ ] 游戏资源加载无报错（probe-stargaze / probe-mobile-tutorial E2E 全绿）
- [x] 未改 `src/systems/StorySystem.ts` / `src/data/` / `maps/` / `tiles/`
- [x] 未改 顶层设计.md / 剧情开发规划.md / 林澈圣经.md
- [x] `npx tsc --noEmit` 通过
- [x] `npm run build` 通过

### 6.2 逐项验收
- [x] **3.1 UI 套件**：HUD emoji 全部替换为像素图标；对话框/任务/背包/商店面板视觉统一；移动端硬约束保持
- [x] **3.2 林澈头像**：512×512 RGBA 透明背景；接入至少一处 UI（待制作人定）— 制作人拍板"裁切带背景"：`linchen_avatar.png`（512×512，从 `linchen_s777001_cfg2.png` c_wider 位裁切）；接线 TitleScene 主角头像角标（桌面 128 / 移动端 96，圆角卡片 + 描边，与 StoryDialogue 风格一致）
- [x] **3.3 夏雅 sprite**：三处使用点形象一致；角色设定符合（橙金短发 / 工装感 / 20 岁）— 已由 `8cd9df2`（v0.5.x 美术风格统一）完成：`npc_xiya` v2 像素风（橙金短发 + 工装背带裤 + 工具腰带），三处共用单一纹理形象一致；farm 教程无夏雅 sprite 为当前实现设计差异，不作为缺陷
- [x] **3.3 夏雅立绘**：如制作人确认升级，512×768 RGBA，沿用 `gen_portrait_final.py` 管线 — 已接入 `PORTRAIT_MAP`（512×768 RGBA）
- [x] **3.4 镇长立绘**：512×768 RGBA；接入 `PORTRAIT_MAP`；米黄长者风 — `elder.png`（512×768 RGBA，来自 `elder_s202608021.png`），`PORTRAIT_MAP` 已加 `镇长→elder.png`；probe-elder-portrait 5/5 全绿

---

## 7. 完成后提交

- 每项单独 commit（3.1 / 3.2 / 3.3 / 3.4 各一次），便于回滚
- commit 信息建议：
  - `art(v0.6): UI 套件像素风统一（HUD/对话/任务/背包/商店）`
  - `art(v0.6): 林澈头像绘制 + 接线`
  - `art(v0.6): 夏雅 sprite 升级（覆盖 gate/farm/E1 三处）`
  - `art(v0.6): 镇长立绘绘制 + 接线`
- 提交前先 `git status` 确认只包含本任务文件
- 同步更新 [CHANGELOG.md](../../CHANGELOG.md) `[未发布]` 区块顶部

---

## 8. 后续（不在本任务内，仅提示）

### 8.1 v0.6 第二批美术（[顶层设计 §10.6 决策③](../../顶层设计.md)）
- 标题界面视觉升级
- 场景氛围（光影 / 天气）
- 其余 NPC 立绘（神秘少女 / 老张 / 小梅 / 阿风 / 商店老板）

### 8.2 缺口待评估（非 v0.6 必做）
- 作物生长阶段 spritesheet（现 `crops.png` 单帧）
- 砍树倒下动画 / 矿脉开采粒子
- 观星点粒子效果（[顶层设计 §10.4](../../顶层设计.md) v0.5.2 允许项未实施）

---

## 9. 变更记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-08-02 | v0.1 | 草案创建：基于 [顶层设计 §10.6](../../顶层设计.md) 决策 + 现有资源盘点，整理 4 项第一批美术需求，待 v0.6 启动 + 制作人确认 |
| 2026-08-02 | v0.2 | 3.1 UI 套件完成（`41a0c02`）：HUD emoji → 像素图标（新增体力/金币图标）；背包/商店/任务面板色板统一；对话框底色对齐色板。移动端硬约束保持。DoD §6.2-3.1 已勾选 |
| 2026-08-02 | v0.3 | 3.3 夏雅 sprite/立绘验收（制作人确认无需增量优化）：已由 `8cd9df2` 完成（v2 像素风 + 三处形象一致 + 立绘 512×768 已接入）。farm 教程无夏雅 sprite 记为实现设计差异，非缺陷。DoD §6.2-3.3 已勾选 |
| 2026-08-02 | v0.4 | 3.2 林澈头像（制作人拍板"裁切带背景"）：`linchen_avatar.png`（512×512，源图 c_wider 位裁切）+ TitleScene 主角头像角标（128/96 圆角卡片）。验证：tsc/build/probe-title-avatar 7/7/probe-mobile-layout 10/11（1 预存失败）。DoD §6.2-3.2 已勾选 |
