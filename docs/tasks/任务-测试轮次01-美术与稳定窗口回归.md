# 任务卡：测试轮次 01（美术与稳定窗口回归）

> 立项：制作人 2026-08-03（开发冻结，进入测试窗口）
> 指派对象：**待定**（制作人安排其他 AI 执行；Codex 仅立项，**不执行测试**）
> 基线：HEAD `bf4cdc6`（main）
> 测试等级：Level 1~2（回归 + 功能验证，不跑全量）
> 状态：⏳ 进行中（批 1 自动化回归 ✅ / 批 3 存档浏览器侧预验证 ✅，批 2 美术目测 + 批 3 真机存档待制作人执行）

### 批 1 自动化回归结果（Trae 执行，2026-08-04）

- tsc 0 错 + **14/14 探针全绿，0 失败**（含任务卡清单 12 项 + probe-mobile-layout/text 补充）
- 明细见 [测试轮次01-美术与稳定窗口回归.md](../reports/测试轮次01-美术与稳定窗口回归.md)
- 风险清单核对：①农田成熟态统一萝卜（已知限制，待批 2 目测确认观感）②新头像裁切（probe 加载正常，观感待批 2）③音效真机 fps（待批 3）④GuiXingRecordSystem 在途未提交（跳过）⑤CDP 竞态 0 次
- 结论：批 1 通过，无 BUG-037+ 新增

### 批 3 存档专项浏览器侧预验证（Trae 执行，2026-08-04）

- **24/24 断言全绿**（`node tests/probes/probe-save-restore.mjs`），真实游玩路径：新档 → 车站 → 大门 → 钥匙 → 农场锄/种/浇 → 教程完成（床边 E，day=2 清晨）→ reload 模拟杀进程 → 恢复
- 验证覆盖：恢复进游戏（非 station/title 重开）、storyStep 保持 done（教程不重开）、无残留对白、存档 day/storyStep/inventory/每日任务 reload 前后一致、任务面板/背包面板打开且渲染正常（切 farm 验证，恢复点在 house 床上无面板属正常设计）
- 结论：**存档杀进程恢复逻辑浏览器侧全链路无异常**，真机环节仅需复验横竖屏/摇杆避让/任务面板安全区/返回键层级 + 存档恢复抽查
- 探针：`tests/probes/probe-save-restore.mjs`
- 真机检查清单：[批3真机存档专项检查清单.md](../reports/批3真机存档专项检查清单.md)

---

## 0. 背景

最近 7 个 commit 积压了一批变更，需要一轮完整回归 + 目测验收：

| 类别 | 内容 |
|---|---|
| 美术替换 | 镇长 AI 头像（`d1f957a`）、林澈方向 B 头像/立绘（`23c759d`）、**农田五态地块接入**（`bf4cdc6`，平色矩形 → farm_plot 5 帧贴图） |
| 功能 | 环境音效完善 + 归星记录设计定稿（`244536c`）、BUG-035/036 修复、横屏提示层、基线探针修复（`cbe07b6`/`e20e167`） |

## 1. 范围

- ✅ **测**：HEAD 已提交内容（`bf4cdc6` 及之前）
- ⛔ **不测**：工作区未提交在途（`GuiXingRecordSystem.ts`、体验值登记报告、FEATURE-036 机器人入口报告、相关探针改动）——如测试前归属 AI 已提交则顺带回归，未提交则跳过，避免半成品噪音

## 2. 批次与清单

### 批 1 · 自动化回归（约 30~40 分钟）

前置：`npm run dev` 起服务（localhost:5173）

```powershell
npx tsc --noEmit
node tests/probes/test-tutorial.mjs          # 新手全流程（含农田五态帧切换）
node tests/probes/probe-farm-restore.mjs     # 花园恢复 + 农田回归
node tests/probes/probe-elder-portrait.mjs   # 镇长新头像接线
node tests/probes/probe-mobile-tutorial.mjs  # 移动端教程
node tests/probes/probe-mobile-ux.mjs        # 移动端布局
node tests/probes/probe-bug030-034.mjs       # 触控/横屏
node tests/probes/probe-rotate-hint.mjs      # 竖屏横屏提示层
node tests/probes/probe-ambience.mjs         # 环境音效
node tests/probes/test-ch1-story.mjs         # 第一章剧情
node tests/probes/probe-first-hour.mjs       # 首小时体验
node tests/probes/probe-density-v053-batch2.mjs  # 剧情密度（爷爷笔记 4 条轮换断言已同步）
node tests/probes/probe-garden-xiya.mjs          # 花园夏雅对白（5 行，推进上限已同步）
```

> ⚠️ 批 1 前请先跑 `npx tsc --noEmit`；本清单已含《序章世界观伏笔补强方案》受影响探针（2026-08-03 文案改动后同步）。

### 批 2 · 美术目测（制作人试玩，约 20 分钟）

1. **对话头像**：林澈（晨光庄园）与镇长新头像同屏协调性、方形裁切（96/128）
2. **农田五态**：锄地→播种→浇水→成熟全链路观感；**注意成熟态目前统一显示萝卜**（番茄/玉米/草莓视觉待作物贴图批次，先登记不修复）
3. 花园恢复视觉 / NPC 生活动作 / 环境音听感（风/鸟/虫鸣音量与层次）

### 批 3 · 移动端 + 存档专项（真机，约 20 分钟）

- 横竖屏、摇杆位置（左 48px 避让）、任务面板安全区、返回键层级
- 存档：睡觉保存 → 杀进程 → 重开恢复；跨天不丢已完成未领奖任务

## 3. 风险清单（重点盯）

1. 农田成熟态统一萝卜（非萝卜作物视觉，已知限制）
2. 新头像方形裁切后脸部是否完整
3. 环境音效真机性能（farm 场景 ≥30fps）
4. 在途 `GuiXingRecordSystem` 未提交，相关探针不在清单内
5. 探针偶发 CDP 导航竞态（复跑即恢复，非游戏 bug）

## 4. 产出要求

- 每批结果汇总；新问题按 **BUG-037+** 编号登记到 `问题追踪.md`
- 汇总报告：`docs/reports/测试轮次01-美术与稳定窗口回归.md`
- 截图存 `tests/probes/test-screenshots/`（已被 gitignore，不入库）

## 5. 红线

- **只测不改**：发现问题登记，不顺手修复（P0 黑屏/坏档除外）
- 不提交其他 AI 在途工作
- 不动存档结构、不改美术资源
