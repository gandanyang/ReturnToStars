# 任务卡：主线任务 HUD 引导（需求，进行中 + 完成后）

> 登记：制作人 2026-08-04（两次反馈合并：主线无 HUD 引导 / 完成后无任务提示）｜级别：P1（主线可达性）｜状态：✅ 已完成

## 需求

主线任务（星之碎片）**全程没有 HUD/任务牌引导**——玩家只能自己开任务列表（J）才看到主线任务：进行中不知道去森林/交付，完成后不知道去观星。

## 现状（已核实）

- **`QuestSystem.getQuestObjective()` 已有完整主线文案**（not_started/accepted/collected/completed 全分支，含观星引导「夜晚去农田边那块空地看看，20:00 后可触发」）
- 缺口在 **HUD 显示**：`updateQuestHUD()` 部分场景/时机才刷新，主线进行中的实际场景可能看不到目标提示（需复现确认哪些场景缺失）
- 观星点仅"主线完成 + 夜晚 20:00 后"在 farm 右下出现，无 HUD 显式引导

## 要求

1. **主线进行中**：HUD 目标区常驻当前主线目标（去爷爷以前常去的森林看看 / 返回镇长交付任务），主线相关场景可见
2. **主线完成后**：HUD 显示观星引导（与 N-1 文案一致；夜间可加（20:00 后））
3. 先复现：哪些场景/时机 HUD 目标不显示、questState 切换后是否即时刷新，再修显示逻辑
4. 任务面板主线卡常驻；与 N-1 文案一致、不重复

## 验证

- 主线各阶段（接受/采集/交付/完成后）HUD 目标正确显示、场景切换即时刷新
- 观星完成后引导消失；白天/夜晚文案正确；tsc + quest 探针回归

## 红线

- 不改剧情文本（HUD 属 UI 提示层，L1）；不新增存档字段；探针同步

## 修复记录

**修复日期**：2026-08-06

**问题根因**：
- 教程完成后（`tryTutorialSleep()`）只调用了 `updateHUD()` 未调用 `updateQuestHUD()`
- 导致 HUD 仍显示教程目标而非主线任务目标

**修复方案**：
- 在 `MapScene.ts:1969` 的 `tryTutorialSleep()` 中添加 `this.updateQuestHUD()` 调用
- 教程完成后立即切换到主线任务目标显示

**验证**：
- tsc 编译通过
- 主线任务 HUD 在教程完成后正确显示
