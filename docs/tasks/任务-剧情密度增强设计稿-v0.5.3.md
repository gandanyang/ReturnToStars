# v0.5.3 剧情密度增强设计稿

> 类型：设计稿（实现输入）
> 依据：《任务-剧情密度增强规划-v0.5.3.md》（任务书，制作人 2026-08-02 定稿）
> 作者：opencode（剧情 Agent）
> 版本：v0.5 | 2026-08-02
> 状态：✅ 六事件全部实现并验证完成（含引导剧情增强 §6c）；四要素规范已落地
> 开发约束：单人开发——全部采用「纯数据 + 最小钩子」，不新增系统、不新增存档字段、不改主线

---

## 0. 设计总纲

**核心目标**：让玩家感觉「我不是在完成任务，我是在这里生活」。

**四条红线**（贯穿所有事件）：

1. **小触发 > 大回忆**：一句话、一个动作点到即止，禁止闪回
2. **不改变主线**：叠加在现有教程/主线之上，不加任务链
3. **不进入复杂存档系统**：全部无状态或内存态，零新增存档字段
4. **不违反角色圣经**：每句台词过《林澈圣经.md》与《剧情开发规划.md》§2.1 角色声音

**工程化四要素**（设计新事件必查，缺一不可，见《剧情开发规划.md》剧情生成规则）：

1. **触发条件**：用什么现有系统值触发？一次还是可重复？状态如何复位？
2. **场景生命周期**：场景切换（`scene.start`/`SHUTDOWN`）时是否残留？是否需要清理？
3. **是否影响存档**：零新增字段；进存档需升 `SAVE_VERSION` 并兼容老档
4. **是否影响测试节点**：会不会改既有探针断言？是否需同步/新增探针？

**四要素未答清前，不得进入实现。**

**实现原则**（单人开发友好）：

- 事件全部是**追加对白**（新增 `DialogueLine`），不碰现有剧情数据流
- 触发条件用**现有系统现成值**：`getTime()`（时间/天数）、`questState`、`isTutorialDone()`、`getStoryStep()`
- 随机对白用**无状态伪随机**（seed = day），避免存档依赖

---

## 1. 事件总览（按优先级）

| # | 事件 | 方向 | 触发 | 涉及NPC | 实现难度 |
|---|------|------|------|---------|---------|
| E1 | 夏雅清晨偶遇 | ② 夏雅日常 | 清晨（06:00-08:00）进入庄园 | 夏雅 | ⭐（数据+1钩子） |
| E2 | 第一次收获反馈 | ② 夏雅日常 | 首次收获作物 | 夏雅 | ⭐⭐（收获钩子） |
| E3 | 林澈"你会修这些" | ① 林澈个人线 | 矿洞首次对话 | 老张 | ⭐（纯数据） |
| E4 | NPC 每日随机一句 | ③ 小镇生活 | 每天首次对话 | 老张/小梅/阿风 | ⭐⭐（随机池+防重复） |
| E5 | 爷爷笔记 | ④ 星星线索 | 观星点可读 | 无（物件） | ⭐⭐（新交互物） |
| E6 | 少女追加一句 | ④ 星星线索 | 观星夜后再次对话 | 神秘少女 | ⭐（纯数据） |

---

## 2. 事件详情

### E1 夏雅清晨偶遇（② 方向 · 优先级 P0）

**目标**：玩家早起时发现夏雅已经在庄园——「她也这么早」，建立陪伴感，不是任务。

**触发**：`getTime().hour >= 6 && getTime().hour < 8` 且玩家进入 `farm` 场景、`isTutorialDone()` 为 true（教程完成后才出现，避免打扰新手引导）。

**内容**（追加到夏雅对话之前，一次触发后当天不再出现）：

- 夏雅：这么早？我睡不着，就过来看看这些地。
- 林澈：你每天都起这么早？
- 夏雅：（笑）岛上的人都这样。太阳一出来，就想醒着。
- 林澈：……我以前，都是被闹钟叫醒的。

**玩家体验**：第一个画面是"有人比你更早、更了解这片地"，庄园开始像一个"有人住的地方"。

**实现难度**：⭐ 低。数据上新增 `XIYA_DAWN_DIALOGUE`；钩子 = 在 `MapScene` 进入 farm 且时间匹配时，把该对话插到夏雅对话列表头部（一次插队），用 `isTutorialDone()` + 时间窗 + 当天 `getDay()` 防重复。

---

### E2 第一次收获反馈（② 方向 · 优先级 P1）

**目标**：玩家第一次把种子种成作物、亲手收获，夏雅给出自然反馈——「屏幕外的果实，比屏幕里的更实在」。

**触发**：玩家第一次收获任一作物（`FarmState` 作物成熟→收获事件，需要确认现收获逻辑在哪）。

**内容**（一次性，收获后播放）：

- 夏雅：这就是你种出来的东西。
- 夏雅：比屏幕里的，要实在一点吧。
- 林澈：（低头看了看手里的萝卜）……确实。

**玩家体验**：种田的第一次正反馈由人给出（而不是系统弹窗），"成果被看见"的感觉。

**实现难度**：⭐⭐ 中。需要找到收获代码位置，在收获成功后触发一次对话（内存 flag 防重复）。这是四个方向里唯一要碰"核心玩法逻辑"的，单人开发要注意**不动收获本身的逻辑**，只加触发。

---

### E3 林澈"你会修这些"（① 方向 · 优先级 P1）

**目标**：用一句话让玩家知道"他过去很厉害"，不解释、不卖惨、不展开。

**触发**：矿洞第一次与老张对话（老张对话第 4 条「这矿里挖出来的东西，比你见过的所有代码都老」之后自然接续）。

**内容**（追加到 `MINER_DIALOGUES` 末尾）：

- 矿工老张：……说起来，这矿里老旧的机器，镇上没人会弄。
- 林澈：以前工作的时候，经常处理这些。
- 矿工老张：哦？那你可帮大忙了。
- 林澈：（笑了笑，没接话）

**玩家体验**：一句"以前工作的时候"——不解释过去，但玩家自己补全了"他过去很专业"。人物空白被尊重。

**实现难度**：⭐ 低。纯数据追加到 `MINER_DIALOGUES`，不碰任何逻辑。是否要在矿洞放一台可修的机器属第二章内容，**本版本只做台词**。

---

### E4 NPC 每日随机一句（③ 方向 · 优先级 P1）

**目标**：NPC 像真实居民——不每句都服务剧情，让玩家愿意每天去搭话。

**触发**：每天与该 NPC 的**首次对话**时，在固定对话之后追加一句随机生活台词。

**随机池**（每 NPC 3-4 句，轮换 + 当天固定）：

| NPC | 随机池 |
|-----|--------|
| 老张 | 今天风不错，适合晒木材。 / 今年雨水比去年多，地倒是好挖了。 / 昨晚听见林子里有动静，估计又是野猪。 |
| 小梅 | 今天这花开得比昨天好。 / 听说庄园里种出了新作物？ / 我的水壶漏了，正愁呢。 |
| 阿风 | 森林最近有奇怪的声音，我可没说谎。 / 我今天又发现一个没人去过的地方。 / 明天想去北边看看，你去不？ |

**选句规则**：`seed = 当天天数 + NPC id 的 hash`，取模选 1 句，同一天同 NPC 固定同一句（读档回来不跳变，因 seed 只依赖天数——**不依赖存档**）。

**玩家体验**：每天搭话有小变化，NPC 从"站桩发布信息"变成"会聊天的邻居"。

**实现难度**：⭐⭐ 中。需要改 `MapScene` 的 NPC 对话触发处：加一个"当天已说过随机句"的**内存标记**（`Map<key, day>`），对话时插入。不改 `NPC.ts` 数据结构、不碰存档。

---

### E5 爷爷笔记（④ 方向 · 优先级 P2）

**目标**：给"星星是什么"积累碎片线索，不解释。

**触发**：观星点（或庄园角落）新增一个**可读物件**，靠近按 E 阅读爷爷的笔记（纸面风，`speaker: '爷爷的信'` 颜色）。

**内容**（笔记片段，多条可轮换）：

- 爷爷的笔记：今天又捡到一片。星星……是不是也想回家？
- 爷爷的笔记：我数了数，还差一些。等它们都回来了，也许就能问清楚了。
- 爷爷的笔记：那些发光的碎片，醒来时像在看我。是我多心了吧。

**玩家体验**：没有解释，只有爷爷的日常记录。玩家开始把"碎片"和"爷爷为什么不在了"联系起来，但一切都是猜测。

**实现难度**：⭐⭐ 中。需要在庄园/观星点放置一个可交互物件（复用车/树/矿脉的 `tryInteract` 交互模式），复用 StoryDialogue 播放。不新增系统，但要在 MapScene 加一个"物件→对话"的数据条目。

---

### E6 少女追加一句（④ 方向 · 优先级 P2）

**目标**：让神秘少女在观星夜之后"还在关注这件事"，制造长期期待，不揭秘。

**触发**：观星夜完成后（`getStoryStep() === 'observatory_complete'`）再次与少女对话，在其固定对话之后追加。

**内容**（追加到 `MYSTERY_DIALOGUES`）：

- 神秘少女：你捡到的那片……它也认识你了。
- 林澈：你也捡到过？
- 神秘少女：（没有回答，只是看着天空）……快归位了。

**玩家体验**：少女始终比玩家知道得多，但只给"快归位了"这种模糊信号——悬念累积，不揭底。第二章内容全部保留。

**实现难度**：⭐ 低。纯数据追加 + 一个 `getStoryStep()` 条件判断。

---

## 3. 优先级与批次（单人开发排期）

| 批次 | 事件 | 预估工作量 | 依赖 |
|------|------|-----------|------|
| 批次 1（P0） | E1 清晨偶遇 + E3 老张一句 | ~1 天 | 无 |
| 批次 2（P1） | E4 NPC 每日随机 + E2 收获反馈 | ~1-1.5 天 | 需要确认收获代码位置 |
| 批次 3（P2） | E5 爷爷笔记 + E6 少女一句 | ~1 天 | 无 |

**建议顺序**：先做"纯数据"的 E3/E6（零风险、可立即验证台词），再做 E1/E4（需要小钩子），最后 E2/E5（碰一点玩法逻辑，风险最高，放最后且有 E2E 护航）。

---

## 4. 涉及文件

| 文件 | 改动 | 风险 |
|------|------|------|
| `src/systems/StorySystem.ts` | 新增 `XIYA_DAWN_DIALOGUE` / 追加 `MINER_DIALOGUES`、`MYSTERY_DIALOGUES` / 新增 `GRANDPA_NOTES` | 低（纯数据） |
| `src/systems/NPCSystem.ts` | 若随机句按 NPC 归属，可放此处或留在 MapScene | 低 |
| `src/scenes/MapScene.ts` | E1 插队钩子 / E4 每日随机插入 / E5 可读物件 | 中（新增交互点，用现有模式） |
| `src/data/TimeSystem.ts` | 无改动（复用 `getTime`） | — |
| 存档 | **零新增字段** | — |

---

## 5. 玩家体验路径示例

```
Day 2 清晨（06:30）进入庄园
→ 夏雅：这么早？我睡不着，就过来看看这些地。
→（对话结束，各自忙去——没有任务、没有奖励）

Day 2 午后，第一次收获萝卜
→ 夏雅：这就是你种出来的东西。比屏幕里的，要实在一点吧。

Day 3 去矿洞找老张
→ 老张：……这矿里老旧的机器，镇上没人会弄。
→ 林澈：以前工作的时候，经常处理这些。

Day 3 去小镇搭话
→ 小梅：今天这花开得比昨天好。
→（明天再来，说的是另一句）

观星夜后，再去见神秘少女
→ 少女：你捡到的那片……它也认识你了。……快归位了。
```

玩家体感：**每天有人等你、有人聊天、有人比你更懂这座岛**——但没有任何任务强迫你。

---

## 6. 验收清单（实现后）

- [x] E1 清晨偶遇：仅 06:00-08:00 且教程完成后触发，当天不重复，不影响新手引导 ✅ probe-density-v053 E1a-d
- [x] E2 收获反馈：仅第一次收获触发，收获逻辑本身无变化 ✅ probe-density-v053-batch2 E2a-d（内存 flag 防重复）
- [x] E3 老张追加：矿洞首次对话出现，不打断主线 ✅ probe-density-v053 E3a
- [x] E4 NPC 随机句：同一天同 NPC 固定一句，次日换句；读档不跳变 ✅ probe-density-v053 E4a-d + 阿风完整对话人工核读
- [x] E5 爷爷笔记：可读、多条轮换、无解释 ✅ probe-density-v053-batch2 E5a-b（按天轮换，无新增存档字段）
- [x] E6 少女追加：仅观星夜后出现 ✅ probe-density-v053-batch2 E6a-d（`isObservatoryComplete()` 条件 + 未观星对照）
- [x] 全部：`npx tsc --noEmit` 通过；tutorial / ch1 E2E 仍全绿；无新增存档字段 ✅

---

## 6b. 第一批（E1/E3/E4）体验验证结论（制作人试玩前）

> 验证方式：`probe-density-v053.mjs`（断言 9/9）+ `probe-density-experience-v053.mjs`（慢速真实阅读）+ 阿风全链路人工核读。

| 事件 | 结论 | 依据 |
|------|------|------|
| **E1 清晨偶遇** | ✅ 通过。清晨进庄园见夏雅，对话自然、点到即止、无任务感；同一天不重复 | E1a-d 全过；对话实录：夏雅"这么早？我睡不着…" → 林澈"……我以前，都是被闹钟叫醒的"（截图 v053-1/2/3） |
| **E3 老张过去** | ✅ 通过。"以前工作的时候，经常处理这些"承接"镇上没人会弄"，林澈一句带过不解释，人物空白被尊重 | E3a 通过；实录：老张"这矿里有些老旧的机器…没人会弄" → 林澈"以前工作的时候，经常处理这些" → 老张"哦？那你可帮大忙了"（截图 v053-5） |
| **E4 每日随机句** | ✅ 通过。老张/小梅/阿风对话末尾都追加了生活句；seed 依赖天数，次日换句、读档不跳变 | E4a-d 通过；阿风 8 句 + "听说老张昨晚又喝多了，哈哈。"；小梅随机句"这株花啊，是我爷爷种的"（截图 v053-4） |

**已确认的事实**：六事件全部实现完毕，均为「纯追加对白 + 最小钩子」，零新增存档字段；与 2fe54c7（他人移动端修复）无冲突，tsc 全绿，tutorial / ch1 E2E 回归无损。

**第二批（E2/E5/E6）已实现并验证**（`85ddd38` + `cf2e260`，`probe-density-v053-batch2` 10/10）：
- **E2 第一次收获反馈**：收获成功钩子 + 内存 flag 防重复，收获逻辑本身零改动，探针 E2a-d 通过（首收触发 / 二次不重复）。
- **E5 爷爷笔记**：庄园左上角可读物件（椭圆 + "笔记"标签），按天轮换（seed=day），复用 StoryDialogue 播放，无新增存档字段。
- **E6 少女追加**：`isObservatoryComplete()` 条件，观星后追加 3 句，未观星对照验证不追加。

**剩余决策项**：六个事件已全部落地，无待实现内容。制作人试玩后若对台词/触发条件有调整意见，按 §7 边界范围内微调即可。

---

## 6c. 引导剧情增强（制作人移动端试玩反馈）

> 制作人移动端试玩反馈："砍树/挖矿引导在移动端体验不到剧情，任务流程有问题"。定位与修复如下（源码 `b259bc2` + `659030e`，探针 `39837d2` 后提交）。

| 问题 | 根因 | 修复 |
|------|------|------|
| 砍树引导被爷爷笔记抢占 | 爷爷笔记 (1,3) 距第一棵树 (2,3) 仅 17.9px；`tryGrandpaNoteInteract`（28px）优先级高于砍树（24px），且交互基准误用 label 坐标（偏移 -8px） | 爷爷笔记移到 (1,6)，交互基准改用椭圆实际坐标 `grandpaNotePos` |
| 引导不像"剧情" | 砍树/挖矿引导原各 3 句（旁白+内心+提示） | 扩为夏雅参与的 7 句剧情对话（符合夏雅"观察→引导"、林澈"冷幽默不炫耀"角色圣经） |
| 挖矿引导触发时机突兀 | 进矿洞任意位置按交互即弹引导，未检查是否靠近矿脉 | 改为仅在矿脉旁（24px）首次交互触发；对话结束后需再按一次开采 |

**验证**：`probe-guide-dialogue`（移动端全流程触屏，6/6）+ `probe-note-vs-woodcut`（笔记可交互 + 树旁不冲突，2/2）+ 回归 test-woodcutting（18/18）、probe-density-v053 / batch2、probe-mobile-tutorial / text、test-ch1-story 全绿。

---

## 7. 明确不做（Scope 边界）

- ❌ 不新增好感度/礼物/亲密度数值系统
- ❌ 不新增任务链、不改变主线剧情
- ❌ 不放置"可修机器"等第二章功能（只做台词铺垫）
- ❌ 不新增存档字段、不改变存档结构
- ❌ 不做天气/季节系统（随机句文案里提到风/雨只是生活化措辞）

---

## 8. 扩展事件（v0.5.3 后续）

### E7 商店老板每日随机句（③ 方向 · 优先级 P1）

**四要素**：

1. **触发条件**：每天首次与商店老板对话时，固定对话之后追加一句随机生活台词。复用 E4 的 `npcDailySaid` 内存标记机制（`Map<npcId, day>`），同天不重复。
2. **场景生命周期**：`npcDailySaid` 是 MapScene 模块级内存态，场景切换时随 MapScene 实例重建归零，不残留。
3. **是否影响存档**：零新增字段。seed = day + npcId hash，不依赖存档。
4. **是否影响测试节点**：需新增探针断言（商店老板对话含随机句）。

**内容**（随机池 4 句）：

| # | 台词 |
|---|------|
| 1 | 今天有批新货到了，来看看？ |
| 2 | 最近买种子的人多了，看来大家都开始种地了。 |
| 3 | 镇上好久没这么热闹了。你来了之后，感觉不一样了。 |
| 4 | 我年轻的时候也种过地，后来……算了，不提了。 |

**玩家体验**：商店老板从"功能NPC"变成"有过去的人"，第4句暗示他也有故事。

**实现难度**：⭐ 低。纯数据追加到 `NPC_DAILY_LINES.shopkeeper`。

---

### E8 镇长每日随机句（③ 方向 · 优先级 P1）

**四要素**：

1. **触发条件**：每天首次与镇长对话时，固定对话之后追加一句随机生活台词。复用 E4 机制。
2. **场景生命周期**：同 E7。
3. **是否影响存档**：零新增字段。
4. **是否影响测试节点**：需新增探针断言。

**内容**（随机池 4 句）：

| # | 台词 |
|---|------|
| 1 | 你爷爷以前每天傍晚都会来我这儿坐坐。 |
| 2 | 这座岛啊，安静太久了。有人回来，挺好的。 |
| 3 | 星星的事……你慢慢来，别着急。 |
| 4 | 今天的天气，适合看星星。 |

**玩家体验**：镇长不只是"发任务的人"，他了解爷爷、关心这座岛、会观察天气。

**实现难度**：⭐ 低。纯数据追加到 `NPC_DAILY_LINES.elder`。

---

### E9 夏雅傍晚关心（② 方向 · 优先级 P1）

**四要素**：

1. **触发条件**：教程完成后，傍晚（18:00-20:00）进入农场时出现夏雅精灵。玩家靠近按 E 播放对话。当天触发过一次后不再出现（`eveningXiyaDay` 记录，跨天重置）。
2. **场景生命周期**：夏雅精灵为 MapScene 内存态，场景切换/跨天时 `clearEveningXiya()` 销毁，不残留。
3. **是否影响存档**：零新增字段。`eveningXiyaDay` 是模块级变量，不进存档。
4. **是否影响测试节点**：需新增探针断言（傍晚农场含夏雅精灵、交互播放对话）。

**内容**（5 句）：

| # | 台词 |
|---|------|
| 1 | （傍晚的庄园染上一层金色。夏雅坐在栅栏边，看着远处的海。） |
| 2 | 今天过得怎么样？ |
| 3 | ……挺累的。但是和以前那种累不一样。 |
| 4 |（笑了笑）这里没有KPI，没有周报。你做的事情，土地都会记得。 |
| 5 | （沉默片刻）……谢谢。 |

**玩家体验**：傍晚的庄园有金色光线，夏雅坐在栅栏边看海，问林澈今天过得怎么样。"你做的事情，土地都会记得"点题——从数据劳动者到真实劳作者的转变。

**实现难度**：⭐ 低。复用 E1 结构，纯数据+触发逻辑。

---

## 9. 变更记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-08-02 | v0.1 | 设计稿起草：6 个事件、4 方向全覆盖、批次排期、涉及文件、验收清单 |
| 2026-08-02 | v0.2 | 第一批 E1/E3/E4 实现并验证完成（断言 9/9 + 体验探针 + 阿风人工核读全通过），验收清单勾选；新增 §6b 体验验证结论，供制作人试玩后决策 E2/E5/E6 |
| 2026-08-02 | v0.3 | 第二批 E2/E5/E6 实现并验证完成（`probe-density-v053-batch2` 10/10），验收清单全部勾选；§6b 更新为"六事件全部实现"，无待决策项 |
| 2026-08-02 | v0.4 | 制作人移动端试玩反馈后增强引导剧情（§6c）：爷爷笔记移(1,6)并修复交互基准、挖矿引导改矿脉旁触发、砍树/挖矿引导扩为夏雅参与的 7 句剧情对话；源码 `b259bc2`+`659030e`，新增探针 `probe-guide-dialogue`/`probe-note-vs-woodcut`，回归全绿 |
