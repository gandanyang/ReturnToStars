# T2 开工门禁：现有种田流程文件清单 + 修改计划

> 制作人 2026-08-06 拍板：T2（E-07/E-08 体验债务）开工前必须先输出本清单，防止"新增引导系统"。
> 原则：**零新系统，全部复用现有轮子**。红线：❌ 新货币/新建筑/新任务链/新UI/新经济公式。

---

## 一、现有种田流程涉及文件清单（全链路）

玩家 Day1 核心循环：**清理 → 播种 → 成长 → 收获 → 出售 → 资源 → 修复**

| 环节 | 涉及文件 | 现有实现 |
|---|---|---|
| **清理**（庄园起步） | `src/scenes/MapScene.ts`（`tryGardenRestoreInteract` ~1020 行）<br>`src/data/FarmRestore.ts` | 花园三阶段清理（M1-3 既有，80px 首次提示已做） |
| **播种** | `src/scenes/MapScene.ts`（`handleFarmTap` 5318 行 / `tryFarmInteract` 5378 行 / 种子选择器）<br>`src/data/FarmState.ts`（`setCrop` / `getTileState`） | 点击格子种田，种子选择器 UI 已有 |
| **成长** | `src/scenes/MapScene.ts`（update 循环作物生长）<br>`src/data/FarmState.ts`（`CropData` / 天数推进） | 每日生长，浇水/生长状态完整 |
| **收获** | `src/scenes/MapScene.ts`（`harvestTileAt` 5523 行 / `harvestPlot` 5651 行）<br>`src/systems/DailyQuestSystem.ts`（卖出计数钩子） | 点击收获进背包 |
| **出售** | `src/ui/ShopPanel.ts`（`sell-all` → `SmartSellPreviewPanel`，291 行 toast）<br>`src/ui/SmartSellPreviewPanel.ts`（FEATURE-039 预览面板）<br>`src/data/Economy.ts`（`previewSellAll` / sellPriority） | 背包/商店智能出售 + 预览面板 |
| **资源** | `src/data/Inventory.ts`（`getItemCount` / `addItem`）<br>`src/systems/SaveSystem.ts` | 木材/石头/金币统一收口 |
| **修复**（循环终点） | `src/data/FarmRestore.ts`（`RESTORE_PROJECTS`：garden/oldHouse/forestRoad）<br>`src/scenes/MapScene.ts`（`markRestored` 调用点）<br>`docs/tasks/任务-FEATURE037-归星岛复兴系统详细任务卡-v0.10.md`（施工中） | 3 建设点：花园清理（无资源）/ 老屋（木30+石20+金100）/ 后山道路（石50+金200） |

**旁路但会用到**：
- `src/systems/StorySystem.ts`（镇长/夏雅对白数据，**冻结区只读**，但新增对白走它）
- `src/ui/StoryDialogue.ts`（对白播放）
- `src/audio/voicebank.data.ts` + `tools/gen_mainline_voice.py`（新对白配音，**优先 MiniMax**）

---

## 二、T2 修改计划（三块，全部复用现有系统）

### 改动 1：Day1 引导链（串起"为什么"）

**目标**：玩家第一天理解"清理→播种→等待→收获→出售→获得资源→修复青禾镇"是一条链。

**实现**（复用 `DailyQuestSystem` 的 guide 注入机制，不加新任务系统）：
- 在现有每日引导 quest 里，**补链式文案**（不是新任务，是现有引导文案强化）：
  - 播种后 → 提示"等它长大，收成能换钱修镇上的旧东西"
  - 收获后 → 提示"这些收成，是镇上老房子的建材费"
- 复用 `QuestPanel` / HUD 现有引导提示位，零新 UI。

### 改动 2：两个关键对白（镇长 / 夏雅）

**目标**：镇长解释"为什么种田"，夏雅解释"为什么小事会改变这里"。

**实现**（在 `src/systems/StorySystem.ts` 新增 2 个 `DialogueLine[]`，触发挂现有节点）：
- **镇长对白**（挂 `ELDER_QUEST_DIALOGUE` 交付后 或 首次到镇触发）：解释这座岛靠作物养活过一镇人、田是岛的心跳——种田不是攒钱，是让地方活过来
- **夏雅对白**（挂 `GARDEN_RESTORED_XIYA_DIALOGUE` 或首次收获后）：解释爷爷怎么用一把锄头把荒地变成家——每一件小事都在让这里"有人气"

**接入方式**：只加数据 + 在 MapScene 现有触发点追加 `play()` 调用（复用 `storyDialogue`），零新系统。

#### 改动 2 台词（制作人 2026-08-06 定稿）

**镇长·为什么种田**（挂 `SHARD_DELIVER_DIALOGUE` 交付完成后追加，即首次交付星之碎片之后）：
```
{ speaker: '镇长', color: COLORS.elder, text: '现在买东西方便了，想吃什么，去店里就能买到。' }
{ speaker: '镇长', color: COLORS.elder, text: '可有时候，人容易忘了一件事。' }
{ speaker: '镇长', color: COLORS.elder, text: '这些东西啊，也不是一开始就在货架上的。' }
{ speaker: '镇长', color: COLORS.elder, text: '一粒种子，要有人种下去，有人照看它，才能变成餐桌上的东西。' }
{ speaker: '镇长', color: COLORS.elder, text: '现在什么都能买到，菜市场有菜，商店有粮。' }
{ speaker: '镇长', color: COLORS.elder, text: '可自己种出来的东西，吃的时候心里踏实。' }
{ speaker: '镇长', color: COLORS.elder, text: '你知道它什么时候种下去，什么时候长出来，也知道这一口是怎么来的。' }
```

**夏雅·为什么小事会改变这里**（挂 `GARDEN_RESTORED_XIYA_DIALOGUE` 首次触发后连播）：
```
{ speaker: '夏雅', color: COLORS.xiya, text: '你有没有发现，最近青禾镇好像比以前热闹了一点？' }
{ speaker: '夏雅', color: COLORS.xiya, text: '其实也没发生什么特别大的事情。' }
{ speaker: '夏雅', color: COLORS.xiya, text: '就是有人把院子收拾了一下，有人种了些花，田里的菜也慢慢长起来了。' }
{ speaker: '夏雅', color: COLORS.xiya, text: '以前我总觉得，要让这里变回来，得做一件很厉害的事情。' }
{ speaker: '夏雅', color: COLORS.xiya, text: '后来才发现，好像不是这样。' }
{ speaker: '夏雅', color: COLORS.xiya, text: '这些小事情多起来了，这里也就慢慢有了人生活的感觉。' }
```

> 挂靠点说明：两段均挂**已有且一次性触发**的对白节点，不新增触发时机/存档 flag；镇长段与夏雅段均为制作人定稿（2026-08-06）。

### 改动 3：出售反馈世界化

**目标**：出售不要只弹"+50G"，要让人感到"这些钱在修复青禾镇"。

**实现**（改 `src/ui/ShopPanel.ts` 291 行 toast + `SmartSellPreviewPanel` 文案）：
- toast 从 `卖出获得 50G<br>萝卜×5` 改为：
  ```
  出售萝卜 ×5
  金币 +50
  ▸ 青禾镇的修复基金增加了
  ```
- `SmartSellPreviewPanel` 底部加一行世界化副文案："这些钱会用来修复岛上的旧设施"
- **纯文案改动**：金币数值、经济公式、出售逻辑一律不动（红线：不碰经济公式）

---

## 三、明确不做（红线清单）

| ❌ 不做 | 原因 |
|---|---|
| 新货币系统 | 现有金币够用 |
| 新建筑系统 | FEATURE-037 3 建设点已覆盖"修复"环节 |
| 新任务链 | 复用 DailyQuest 引导 |
| 新 UI 页面 | 复用 ShopPanel / StoryDialogue / QuestPanel |
| 新经济公式 | 售价/需求全不动，只改反馈文案 |
| 好感系统/新地图/战斗/大型农业扩展 | 制作人冻结 |

---

## 四、T2 完成后验收

- 新玩家 Day1：清花园 → 播种 → （跳过等待）→ 收获 → 出售看到"修复基金"反馈 → 知道钱能修老屋/后山道路
- 镇长/夏雅 2 段新对白触发正常（含配音，走 MiniMax）
- 回归：probe-farm-restore / probe-daily-event / probe-sell-all / probe-ch1-walkthrough 全绿
