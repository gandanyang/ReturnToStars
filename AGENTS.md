# 归星物语 — 开发指南

> 第0章 Demo 已完成并冻结，进入**第一章「复苏」开发阶段**。所有 AI 开发必须遵守本指南。
>
> **核心原则：稳定 > 新功能，体验 > 系统数量，完成一个闭环 > 添加十个半成品。**
>
> **第一章最高指导**：`docs/第一章开发总纲.md`（所有 Agent 以此为准）。

## 开工必读（每个任务开始前）

先读：

1. `docs/AI_CONTEXT.md`（项目上下文 + 平台/设备约束：**移动端只支持横屏**）
2. `docs/AI_GUARDRAIL.md`（强制红线：横屏规则 / UI 测试纪律 / 开工三问）
3. `docs/AI开发护栏-核心产品方向与成长循环-v1.0.md`（产品方向护栏：核心循环 / 功能审核 8 问 / 轻量优先 / 世界变化可感知）
4. `docs/DESIGN_DECISIONS.md`（制作人决策库：已拍板项 Agent 不得推翻）
5. `docs/AI开发前必读.md`（门禁，3 分钟）
6. `docs/开发约束与架构入口.md`（架构入口 + 新增系统检查清单）
7. `docs/第一章开发总纲.md`（第一章阶段定位 / 优先级 / 禁止事项 / 验收标准）
8. 对应任务文档（`docs/tasks/…`）
9. `docs/dev/EventSystem.md`（事件系统契约：**triggerOnce 先执行 fn 后标记 → save/check 放 triggerOnceIf 之后**，涉及一次性事件必读）
10. `docs/dev/TestSystem.md`（**自动化测试体系契约**：L1 回归 / L2 探针 / L3 showcase 三层结构 + harness API + 编写范式 + 测试纪律 + 决策流程；**涉及测试 / 写探针 / 改动验证必读**）
11. `docs/dev/MapExpansion.md`（**地图扩容与底图施工规范**：四字段铁律（map/layer 尺寸 + data 长度 + tileset 引用必须同步）+ tileset 数据源唯一 + Blockout 阶段划分 + GID 漂移验收；**涉及改地图尺寸 / 铺底图 / 新增地图必读**）
12. `docs/AI出图与生视频经验归档-v1.0.md`（出图/生视频调用经验与制作人纪律；**涉及生图/生视频任务必读**）

> ⚠️ **移动端只支持横屏**：禁止按竖屏手游设计、禁止做竖屏适配、禁止用竖屏模拟器验收。详见 `AI_CONTEXT.md` / `AI_GUARDRAIL.md`。

## 第一章开发阶段（2026-08-12 制作人拍板，解除 Alpha Freeze）

> 第0章（Demo，观星夜收尾）已冻结。开发进入**第一章「复苏」**，从"证明玩家愿意回来"转向"证明玩家愿意留下"。
> 最高指导文档：`docs/第一章开发总纲.md`。

**当前版本**：`v0.6-alpha-freeze`（标签保留；开发重心已切换）
> 版本口径（D-034）：产品版本=`v0.6-alpha`（第0章冻结）→`v0.7`（第一章开发）→`v0.8`（第一章完整体验）→`v0.9`（首发候选）；内部 `v1.x` 仅指设计文档版本，与产品版本分开，不混用。

**阶段目标**：体验深化。建立 **玩家行为 → 世界变化 → 情感反馈** 循环，让玩家第一次真正相信"这个小镇可以因为我的到来而慢慢变好"。

### 开发优先级

- **P0 世界反馈**：任何玩家行为都应产生可见反馈（种植→小镇资源/居民生活/区域变化；修复→场景/NPC/氛围变化）
- **P1 NPC 生命感**：已有 NPC 拥有日常行为 / 小事件 / 情绪变化 / 对小镇变化的反馈
- **P2 轻量玩法**：收集 / 采集 / 钓鱼 / 小型互动——必须服务生活感，不引入复杂数值系统

### 判断原则

- 判断标准不是"这个功能很酷"，而是"**玩家是否因此更喜欢这个小镇**"
- 一个功能若不能增强"人与小镇的连接"，优先级降低

### 禁止

- ❌ 无限扩张：大地图 / 新大陆 / 大量 NPC / 复杂剧情线
- ❌ 偏离核心玩法：战斗系统 / 装备系统 / 抽卡系统 / 复杂制造链
- ❌ 为"看起来丰富"增加内容
- ❌ 第0章内容在第一章开发中回归/变动（第0章体验已冻结，见 DESIGN_DECISIONS D-021/D-023/D-025）

## 优先级

### P0（最高）稳定性

- 修复 bug
- 防止黑屏
- 防止存档损坏
- 防止场景切换异常
- 保证教程流程稳定

### P1 提升玩家体验

- UI 反馈
- 对话体验
- 音效反馈
- 操作手感
- 新手引导

### P2 内容增强

- NPC 剧情
- 小任务
- 美术替换

## ComfyUI 出图规则（硬性，所有 AI 必须遵守）

使用 **ComfyUI 生图**时，**默认使用 `workflow/anima_turboV10.json`**（anima turbo 工作流）。

- 这是唯一默认工作流，除非任务文档明确指定其它工作流。
- 禁止自行新建/选用其它 ComfyUI 工作流作为默认（避免风格不统一、参数混乱）。
- 出图任务开始前先确认 `workflow/` 下文件，默认加载 `anima_turboV10.json`。
- 出图/生视频前先读 `docs/AI出图与生视频经验归档-v1.0.md`（GPT 生图文字约束、MiniMax H3 参数与画质经验）。

## 禁止事项（非常重要）

未经允许，**不要**：

- ❌ 新增战斗系统
- ❌ 新增抽卡系统
- ❌ 新增大地图
- ❌ 新增复杂养成
- ❌ 重构整个架构
- ❌ 替换 Phaser 技术方案
- ❌ 引入后端
- ❌ 大规模修改已有系统

## Git 写权限规则（2026-08-06 事故后新增，制作人拍板）

多 Agent 并行开发时，**一个仓库同一时间只允许一个 Agent 持有 Git 写权限**。

- 允许（无 Git 写权限时）：改文件、提方案、写文档、跑探针（只读验证）。
- 禁止（未持有 Git 写权限时）：`commit`、`push`、`gc`、`rebase`、`reset`、`git add` 之后的大段操作。
- **严禁多个 Agent 同时执行 `git add` / `git commit` / `git gc` / `git reset`**——2026-08-06 的 git 对象库损坏事故（提交对象丢失、仓库一度不可写）即由并发 Git 操作引发。
- Git 写操作前先检查是否有其他 Agent 正在执行 git 进程（`Get-Process git`）；有则等待或协调。
- 事故恢复基线：`556230b`（2026-08-06 根提交快照，tag `disaster-recovery-20260806`）。

## Git 协作规则（硬性，2026-08-06 并发暂存事故后新增）

1. **禁止自动执行** `git add -A` / `git add .` / `git commit`（工具链若有自动提交/自动暂存功能，必须关闭，改为人工确认）。
2. **提交前必须确认** `git status`：核对本次要提交的只有自己改的文件。
3. **只提交自己修改的文件**：`git add <指定文件>`（逐个列路径）；**禁止** `git add -A` / `git add .` / 目录级宽泛暂存。
4. **提交统一由 opencode 或制作人执行**：其余 Agent（含 Codex）只负责改代码 / 写文档，**禁止执行 `git add` / `git commit`**；任务完成后只向制作人报告「修改文件列表」，由 opencode 或制作人统一暂存与提交。

> 背景：`eff91f6` 提交时，另一 Agent 并发执行 `git add`，7 个他人文件被卷入同一提交——git 暂存区是仓库共享的，宽泛 add 一定会互相踩。

## Git 协作规则 v2（硬性，2026-08-11 提交对象丢失事故后升级，制作人拍板）

2026-08-11 事故（`9f918f2` 提交对象丢失）根因：**Agent 无边界执行 `reset` 类操作，把他人刚提交的成果回退**。连续两次同类事故（08-06/08-11）证明需要权限边界。

1. **高危命令禁止 Agent 执行**（除非制作人明确批准）：`git reset` / `git clean` / `git checkout .` / `git restore .` / `git rebase`。
2. **Agent 提交后发现问题禁止回退**：一律用 `git revert`（可追踪、保留历史），**禁止 reset 回退**。
3. **不允许多个 Agent 同时操作 main**：并行开发时各 Agent 在独立分支/或严格错峰，最后统一合并（提交仍由 opencode/制作人执行）。
4. **重要阶段打 tag**（恢复锚点）：每个里程碑打 tag，录视频/发版前必须打。
5. 任何 Agent 执行危险命令前：先检查 `.git/index.lock` + 活跃 git 进程（见「资产安全三规则」规则 2），并确认工作区无他人未提交成果。

## 资产安全三规则（2026-08-11 制作人拍板，docs 事故后新增）

### 规则 1：docs 禁止直接删除

`docs/`（design / decisions / character / task / 全部子目录）是项目资产，相当于游戏公司的 **GDD + 会议纪要 + 产品决策记录**，不是垃圾文件。

- **禁止**：直接 `rm` / `git rm` / 移动 docs 下的任何文件（即使只删一个）。
- 删除 docs 文件必须：
  1. 提交说明（为什么删、删什么）
  2. 明确完整路径
  3. **等制作人确认**后才可操作
- 归档旧文件用 `archive/`（复制而非删除），禁止物理删除。

### 规则 2：重大 Git 操作必须串行

危险命令（**禁止多 Agent 同时执行**）：

```
git checkout / git restore / git reset / git clean / git rm / git tag / git branch
```

- 执行前必须先检查：`.git/index.lock` 是否存在 + 有无活跃 git 进程。
- 有锁/有进程 → **停止等待**，绝不强制删除锁（`index.lock` 残留 = 事故信号，08-11 docs 消失事故即由锁冲突引发）。
- 本规则覆盖所有 Agent（WorkBuddy / opencode / TRAE / Codex）。

### 规则 3：定期打 tag（恢复锚点）

每个重要阶段打 tag，例如：

```
v0.6-before-video      # 录视频前的基准版本
v0.6-release-candidate # 发版候选
v0.6-demo              # Demo 发布版
```

- 打 tag 前确认工作区为**预期状态**（`git status` 核对）。
- 意义：即使工作区被误删/损坏，一条 `git checkout <tag>` 即可恢复。
- 与 `disaster-recovery-20260806`（事故基线 tag）同级使用。

## 核心文件单写入原则（硬性，2026-08-15 制作人拍板）

> 背景：多 Agent 并行开发中，`MapScene.ts` / `SaveSystem.ts` / `EventManager.ts` 等已成为核心文件，两个 Agent 同时改会造成**排查冲突比写功能还难**。制作人拍板立"单写入原则"。

**规则：同一时间，一个核心文件只允许一个 Agent 持有写入权。**

1. **核心文件清单**（当前）：`src/scenes/MapScene.ts`、`src/systems/SaveSystem.ts`、`src/systems/EventManager.ts`。后续随架构演进可增删，但**默认从严**。
2. **标准流程（流水线式，不同 Agent 交错但不同时碰同一核心文件）**：

   ```
   设计 Agent → 任务卡 → 实现 Agent → 测试 Agent → 合并
   ```

3. **开工前**：先确认没有其他 Agent 正在改你要碰的核心文件（检查活跃进程/工作区状态）；若不明确，先沟通协调，不要直接开工。
4. **只碰自己任务范围的段落**：即使某核心文件允许你写，也只改与当前任务相关的段落，禁止大面积无关修改。
5. **提交仍由 opencode/制作人统一执行**（沿用 Git 协作规则：不自动 `git add`/`commit`）。
6. 违反=与 Git 并发事故同级别的协作事故风险，计入责任。

## 存档版本红线（SaveSystem，2026-08-17 制作人拍板）

**`SAVE_VERSION` 是存档格式契约，改动 = P0 敏感。禁止 Agent 自作主张升级版本号或移除存档字段。**

四条硬性原则（详见 `docs/开发约束与架构入口.md` §核心原则.2）：

1. **新增字段优先「可选 + 默认值」兼容**（`?` + apply `?? 兜底`），此模式**无需升版本号**。
2. **只有破坏性结构变更才递增 `SAVE_VERSION`**；仅加字段严禁升版本。
3. **迁移清档前必须备份旧档**（`return_star_save_backup_<oldVersion>`），禁止静默 `removeItem`。
4. **禁止写入非法 `player.scene`**（空值 save 会被拒绝并返回 false）；开发工具改状态走 `devSave()`，不得写 `save({x:0,y:0,scene:''})`。

> 必须先升版本时：写真实逐字段迁移 + 备份 + 制作人批准。

## 架构问题处理

如果发现架构问题：**先提出方案，不直接重构。**

## 修改代码规则

每次修改前：

1. 先阅读相关文件
2. 理解现有数据流
3. 找到最小修改点

不要"为了方便"创建重复系统。例如：

- 已有 `Inventory.ts` → 不要创建 `NewInventory.ts`
- 已有 `StorySystem.ts` → 不要创建 `DialogueManager2.ts`

## 输出要求

### 每次执行任务前

先说明：

- 你理解的问题是什么
- 准备修改哪些文件
- 为什么这样修改

### 修改完成后

说明：

- 修改内容
- 是否影响存档
- 是否需要测试
- 测试结果

## 测试要求

遵循 **TEST_RULES.md**（AI 测试规范）：按修改影响范围分级测试（Level 0-3），禁止默认运行全部测试。

- 每次修改前：先分析影响范围，判断测试等级
- 测试必须回答："这个修改可能破坏什么？"
- 开始测试前说明：修改内容 / 影响文件 / 风险 / 计划测试 / 预计耗时
- 常见等级参考：
  - 纯文本/文档/注释修改 → Level 0（仅检查格式）
  - UI/数值/单个系统小修改 → Level 1（tsc + 相关模块）
  - 新功能/核心逻辑修改 → Level 2（相关系统测试 + 关键流程）
  - 大版本/存档结构/核心重构 → Level 3（完整测试）

> 不要只说"应该没问题"，**必须验证**。

## 语音交付流程（制作人 2026-08-05 拍板）

> 配音优先使用 MiniMax T2A v2（详见 `docs/MiniMax语音生成工具手册.md`），VoxCPM 仅作离线备选。
> ⚠️ 禁止使用 MiniMax voice_design 接口（单次 9.9 元，2026-08-13 制作人拍板）。新角色音色改用本地 TTS（IndexTTS 等）或已有音色克隆。

语音 / 配音相关改动完成后：

1. **禁止直接打包推包**。先本地生成音频，向制作人提供试听（本机 wav 文件路径，或本地 http 试听 URL）。
2. 制作人确认音色 / 效果满意后，才允许下一步：更新文档 → 打包 APK → 装机。
3. 音色不达预期时：换参考音色 / 调整参数重新生成 → 再次试听 → 确认后再打包。
4. 试听与真机听感可能不同（WebView 音频链路），真机复验仍由制作人在装机后执行。

## 开发原则

- 稳定 > 新功能
- 体验 > 系统数量
- 完成一个闭环 > 添加十个半成品

请把《归星物语》当作一个正在进入 Alpha 阶段的独立游戏项目维护。

## 剧情权限规则

涉及以下内容时：

- 主角成长
- 角色身份
- 世界观真相
- 关键剧情节点
- 玩家选择

执行 Agent 不得自行扩写或改变。

必须遵循：
顶层设计.md > 剧情规划文档 > 实现需求。

## Design Authority

当实现、规划文档、Agent建议冲突时：

优先级：

1. 制作人最终决策
2. 顶层设计.md 当前版本
3. 功能规划文档
4. Agent自主建议

Agent不得自行恢复已废弃字段。

## Decision Override Rule

已提交的制作人决策优先于历史文档方案。

**已由制作人明确拍板的（范畴）：**
- 剧情删改
- 人物设定取舍
- 体验方向
- 资源保留/删除

**优先级高于：**
- 人物圣经旧版本
- Agent 自行判断
- 资源文件现状

**如发现冲突：**
- 不得自行恢复
- 必须提出差异并等待确认

如果新 commit 明确替代旧设计：

旧设计不得恢复。

恢复已废弃方案必须经过制作人重新确认。

> 案例（2026-08-11 阿风台词）：制作人拍板 E1 删除阿风第 4 行，后续 Agent 以「人物圣经 §九 + 语音映射现状」为由自行恢复，导致游戏内台词回退为旧版本。正确做法：发现冲突 → 提出差异（人物圣经依据 vs 制作人拍板记录）→ 等待确认，而非直接恢复。
